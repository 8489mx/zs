# دستور العزل المعماري الصارم وحماية الثوابت الجوهرية
## Z-Systems Architectural Safety & Isolation Constitution

> 🔴 **تحذير إلزامي لكافة نماذج الذكاء الاصطناعي (AI) والمطورين:**
> هذا الملف هو **مرجع دستوري وسيادي لا يجوز خرقه أو تجاوزه بأي شكل من الأشكال**. قبل إجراء أي تعديل على ملفات التوثيق، المصادقة، التوجيه، أو قواعد البيانات، يجب قراءة هذه الوثيقة والالتزام التام بكافة بنودها.

---

## 1. مبدأ العزل المطلق بين البيئات (Zero-Overlap Architectural Boundary)

تمتلك منظومة Z-Systems بيئتين رئيستين مختلفتين كلياً في الهدف والتشغيل:
1. **المنظومة السحابية المركزية (Cloud SaaS):**
   - المعرف الرسمي: `APP_MODE=CLOUD_SAAS`
   - تعمل حصرياً على **سيرفر أوراكل السحابي (Oracle Cloud VPS)**.
   - بنية متعددة المستأجرين (Multi-Tenant Architecture) حقيقية معتمدة على عزل البيانات عبر `tenant_id`.
   - تدير حسابات المنشآت والشركات والمشتركين، بالإضافة للمنصة المركزية (`zs`).
   - **محظور تماماً:** إدخال أي كود أو استثناءات أو افتراضات تخص الديسكتوب، أو حصر البحث في تينانت محلي، أو استبعاد تينانت المنصة (`zs`).

2. **نسخة الديسكتوب المستقلة (Electron / Offline Desktop):**
   - المعرف الرسمي: `APP_MODE=SELF_CONTAINED` أو `IS_ELECTRON=true`
   - تعمل محلياً على أجهزة العملاء عبر قاعدة بيانات محلية منفصلة تماماً (SQLite / Local Postgres).
   - مخصصة للعمل دون إنترنت أو لمتجر واحد (Single-Tenant Standalone).
   - **محظور تماماً:** أن يؤثر منطق الديسكتوب على ملفات السيرفر المركزي أو كود التوثيق العام للساس.

---

## 2. الثوابت الجوهرية والخطوط الحمراء (The 7 Core Invariants)

يُمنع منعاً باتاً كسر أو تعديل أي من هذه الثوابت السبعة:

### الثابت الأول: حرمة حساب وإدارة المنصة المركزية (`zs` / Super Admin)
- المعرف `zs` مخصص حصرياً للمنصة السحابية المركزية ولإدارة المشتركين.
- ممنوع كتابة أي شرط برمجي يستبعد `zs` من عمليات التوثيق أو البحث أو تسجيل الدخول السحابي.

### الثابت الثاني: العزل الصارم لتسجيل الدخول (Tenant-Aware Authentication)
- عند تسجيل الدخول في بيئة الساس، يتم البحث عن المستخدم عبر هويته (اسم المستخدم أو الهاتف أو البريد).
- إذا كان اسم المستخدم فريداً عالمياً (مثل `zs`)، يتم التوثيق وربط الجلسة بمنشأته تلقائياً.
- إذا تكرر الاسم/الهاتف في أكثر من شركة، يُلزم النظام المستخدم باختيار منشأته أو تحديد كود المنشأة (`MULTIPLE_TENANTS` Disambiguation) دون أي تخمين تلقائي.

### الثابت الثالث: سلامة المحرك المالي والمحاسبي (Double-Entry & Immutable Ledgers)
- قيود اليومية، حركات الخزينة، وفواتير المبيعات الصادرة لا تُحذف أو تُعدل بأثر رجعي، بل تتم التسويات عبر قيود عكسية أو إشعارات دائنة/مدينة نظامية.

### الثابت الرابع: عزل المستأجرين في البيانات (Strict Tenant Scoping)
- كل استعلام في قاعدة البيانات (`select`, `update`, `delete`, `insert`) يخص منشأة مشتركة يجب أن يتضمن شرط `tenant_id` المعزول.

### الثابت الخامس: سياسة كلمات المرور والتشفير (Zero Plaintext Passwords)
- كلمات المرور لا تُخزن ولا تُقارن كنص صريح إطلاقاً، بل عبر تشفير `bcrypt` المعتمد مع الـ Salt.

### الثابت السادس: نظافة البيئة السحابية والإنتاجية (Production Env Invariant)
- سيرفر أوراكل الإنتاجي يجب أن يحتوي دائماً وأبداً على:
  - `APP_MODE=CLOUD_SAAS`
  - `PLATFORM_TENANT_ID=zs`
  - `TENANT_ID=zs`
- ممنوع نشر ملف `.env` يحتوي على `SELF_CONTAINED` إلى سيرفر الإنتاج السحابي.

### الثابت السابع: دستور الهوية البصرية والنوافذ المنبثقة (UI & Modal Constitution)
- واجهات عربية RTL افتراضياً (`dir="rtl"`).
- حظر الأيقونات الكرتونية والإيموجيز نهائياً واستخدام أيقونات SVG الرسمية.
- استخدام المكون القياسي الموحد للنوافذ `StandardDialog` و `CustomSelect` بدون تيلويند أو عناصر بدائية.

---

## 3. بروتوكول الذكاء الاصطناعي الإلزامي قبل أي تعديل (Pre-Flight Safety Checklist)

قبل كتابة أي سطر كود، يجب على الـ AI الإجابة بـ **نعم** على الأسئلة التالية:
1. [ ] هل التعديل يحترم بيئة `CLOUD_SAAS` ولا يخلط بينها وبين الديسكتوب؟
2. [ ] هل التعديل يحافظ على إمكانية وصول مستخدم السوبر أدمن `zs` دون عوائق؟
3. [ ] هل التعديل يحافظ على عزل التينانتس (`tenant_id`)؟
4. [ ] هل التعديل يخلو من أي كسر للثوابت المالية والمحاسبية؟
5. [ ] هل تم الامتناع عن تشغيل `npm run build` أو `git push` دون إذن صريح؟

---
**تاريخ الاعتماد:** سبتمبر 2026  
**المرجع:** فريق التطوير والهندسة المعمارية لمنظومة Z-Systems
