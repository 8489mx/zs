# Deployment templates

- `nginx/zsystems.conf.example`: reverse proxy and TLS template.
- `systemd/zsystems.service.example`: service unit template that runs the guarded production startup flow.

Adjust paths, domain names, and certificate locations before production use.
