# Next Steps Execution Plan (SaaS R3 Phase)

Date: 2026-07-30 (Updated)

## الإنجازات السابقة (Completed Steps)
تم الانتهاء بنجاح من كافة خطوات النشر والأمان الأساسية:
- النشر الفعلي على استضافة **Hostinger**.
- ربط قاعدة البيانات المستضافة على **Supabase** مع إعدادات `DATABASE_SSL_REJECT_UNAUTHORIZED=true` تعمل بنجاح.
- دمج وتفعيل **Sentry** لتتبع الأخطاء والمراقبة الحية (Observability).
- عزل المستأجرين برمجياً بنجاح (Multi-tenancy Application Scoping).

## الهدف الحالي (Current Goal: R3 Go-to-SaaS)
التركيز الآن ينتقل بالكامل نحو **العمليات التشغيلية التجارية (Commercial SaaS Ops)** لتبسيط إدارة المشتركين والتحكم الكامل بهم عبر النظام السحابي.

## الخطوة 1 — تحسينات لوحة تحكم الإدارة (Super Admin)
1. مراجعة وتطوير صفحة "إدارة النسخ والمشتركين".
2. عرض حالة كل مشترك بوضوح (نسخة تجريبية Trial vs مدفوعة Paid).
3. تفعيل إجراءات التحكم المباشرة بالعميل (تجديد الاشتراك، الإيقاف، الحذف).

## الخطوة 2 — أتمتة دورة حياة العميل (Automated Provisioning)
1. أتمتة عملية إنشاء مساحة العمل (Tenant/Workspace) بمجرد اشتراك العميل الجديد دون أي تدخل بشري.
2. تفعيل خطط الدفع والفوترة التلقائية (Billing integration) إن لم تكن مكتملة بالكامل.
3. إنشاء تقارير استخدام وإحصائيات عامة للنظام (SaaS Metrics) داخل لوحة تحكم مدير النظام.

## الخطوة 3 — اختبارات الواجهة والأتمتة الشاملة (Frontend E2E Tests)
1. بناء طبقة اختبارات واجهة (E2E Tests باستخدام Cypress أو Playwright) لضمان استقرار رحلة المستخدم في بيئة الـ SaaS.
2. التركيز على اختبار سيناريوهات تعدد المستأجرين من خلال الواجهة.

## تعريف النجاح للمرحلة القادمة
- قدرة مدير النظام (Super Admin) على الإدارة الكاملة للمشتركين بضغطة زر.
- تسجيل عميل جديد، مروراً بالدفع وإنشاء قاعدة البيانات الخاصة به (أو مساحته في الـ Tenant)، يتم ذاتياً في ثوانٍ.
- وجود تغطية اختبارات واجهة أساسية لمنع الـ Regressions مستقبلاً.
