# Frontend cutover checklist

Current release path:
- Build the React frontend to `frontend/dist`
- Serve it through `src/server.js`

Remaining cleanup:
- Legacy vanilla frontend has been removed from the repository
- Keep launch and deployment scripts aligned with the React build only
