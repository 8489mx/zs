diff --git a/backend-new/package.json b/backend-new/package.json
--- a/backend-new/package.json
+++ b/backend-new/package.json
@@ -30,6 +30,7 @@
     "@nestjs/cli": "^11.0.10",
     "@nestjs/schematics": "^11.0.4",
     "@nestjs/testing": "^11.1.0",
     "@types/express": "^5.0.4",
     "@types/node": "^24.9.1",
+    "@types/pg": "^8.15.6",
     "ts-node": "^10.9.2",
     "typescript": "^5.9.3"
   }
 }
diff --git a/backend-new/src/database/migrate.ts b/backend-new/src/database/migrate.ts
--- a/backend-new/src/database/migrate.ts
+++ b/backend-new/src/database/migrate.ts
@@ -4,7 +4,7 @@
 import { Kysely, Migrator, PostgresDialect } from 'kysely';
 import { Pool } from 'pg';
 import { Database } from './database.types';
-import { MigrationProvider } from './migration-provider';
+import { FileMigrationProvider } from './migration-provider';
 
 function getMigrationsPath(): string {
   const distPath = join(process.cwd(), 'dist', 'database', 'migrations');
@@ -29,7 +29,7 @@ async function run(): Promise<void> {
 
   const migrator = new Migrator({
     db,
-    provider: new MigrationProvider(getMigrationsPath()),
+    provider: new FileMigrationProvider(getMigrationsPath()),
     migrationTableSchema: process.env.DATABASE_SCHEMA ?? 'public',
   });
@@ -53,7 +53,8 @@ async function run(): Promise<void> {
   }
 
   if (result.error) {
-    process.stderr.write(`Migration failed: ${result.error.message}\n`);
+    const message = result.error instanceof Error ? result.error.message : String(result.error);
+    process.stderr.write(`Migration failed: ${message}\n`);
     await db.destroy();
     process.exit(1);
   }
diff --git a/backend-new/src/database/migration-provider.ts b/backend-new/src/database/migration-provider.ts
--- a/backend-new/src/database/migration-provider.ts
+++ b/backend-new/src/database/migration-provider.ts
@@ -1,8 +1,8 @@
 import { readdirSync } from 'node:fs';
 import { join } from 'node:path';
-import { Migration, MigrationProvider } from 'kysely';
+import { Migration, MigrationProvider as KyselyMigrationProvider } from 'kysely';
 
-export class MigrationProvider implements MigrationProvider {
+export class FileMigrationProvider implements KyselyMigrationProvider {
   constructor(private readonly migrationsPath: string) {}
 
   async getMigrations(): Promise<Record<string, Migration>> {
