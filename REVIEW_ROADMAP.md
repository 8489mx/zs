# خريطة المراجعة الشاملة والنهائية (Review Roadmap)

> **الغرض:** هذا الملف هو نقطة الدخول الوحيدة لأي جلسة مراجعة جديدة (Opus أو غيره).
> افتح شات جديد، الصق رابط/محتوى هذا الملف، وقول: **"ابدأ بالبند رقم N"**.
> لما يخلص، حدّث حالته هنا، وافتح شات جديد للبند التالي.
>
> **هذا الملف لا يشرح الثغرات التفصيلية** (ده موجود في `ARCHITECTURE_INVARIANTS.md`).
> هو بس بيجاوب على سؤالين: **إيه اللي اتراجع فعلاً وموثوق فيه؟** و**إيه الأولوية التالية؟**

**آخر تحديث:** سبتمبر 2026 · **آخر Commit مرجعي:** `8b1d4270` (20 سبتمبر 2026)
**البنود 9 و 10 و 11 و 12:** ✅ **تم التدقيق والتنفيذ بالكامل بواسطة Antigravity (سبتمبر 2026) — جاهزة لمراجعة وتدقيق Claude Opus.**
البند 1 (عزل المستأجرين) ✅ · البند 2 (O2 — الكتابة بلا `tenant_id`) ✅ ·
البند 3 (منصة SaaS) ✅ · البند 4 (دلتا التوقيت الزمني في الحضور) ✅ · البند 5 (باقي الشحن البحري) ✅ ·
البند 6 (الرواتب وبوابات الموظفين + O20 + O42) ✅ · البند 8 (باقي المقاولات ومخططها) ✅ ·
البند 9 (`pricing` / `crm` / `storefront` + O25) ✅ [Antigravity] ·
البند 10 (`settings` + RBAC + O21, O23, O26, O37, O43) ✅ [Antigravity] ·
البند 11 (`reports.service.ts` + O49) ✅ [Antigravity] ·
البند 12 (`maintenance.service.ts` والترقيم الذري ZM) ✅ [Antigravity]
البند 13 (المتجر — المرحلة 1: الوصول العام للطلبات والتحصيل، SF-1/2/3) ✅ [Claude، 21 سبتمبر 2026، فوق `0473f739`] · البند 14 (المتجر — المرحلة 2: O52–O55 + F28) ✅ [Claude، 21 سبتمبر 2026] · البند 15 (المتجر — المرحلة 3: O56–O60) ✅ [Claude، 21 سبتمبر 2026] — **مراجعة المتجر مغلقة** · البند 16 (سرعة المتجر: الصور خارج الكتالوج — SF-9، هجرة 135) ✅ [Claude، 21 سبتمبر 2026] · البند 17 (الدفع السريع و«اطلب تاني» ورابط التتبع) ✅ [Claude، 21 سبتمبر 2026] · البند 18 (روابط المتاجر على سب دومين — SF-10) ✅ [Claude، 22 سبتمبر 2026، فوق `3418cd30`] · البند 19 (O33 + O27 + O28 + O22 + O36) ✅ [Claude، 23 سبتمبر 2026] · البند 22 (**استعادة كلمة المرور** — الميزة لم تكن موجودة + O72) ✅ [Claude، 23 سبتمبر 2026] · البند 23 (**RPO وتمرين الاسترجاع الآلي** — DEPLOY-7/8) ✅ [Claude، 23 سبتمبر 2026] · البند 24 (**المصادقة الثنائية — TOTP**) ✅ [Claude & Antigravity، 23 سبتمبر 2026] · البند 25 (**الفترات المحاسبية الشهرية وإقفال الشهور — O6**) ✅ [Antigravity، 23 سبتمبر 2026]
— شوف الجدول في القسم 1 و`ARCHITECTURE_INVARIANTS.md` §5-ب و§7 (المراحل 12 → 23).
أي حاجة جديدة تتضاف بعد الكوميت المرجعي، قِسها بـ:
`git diff 8cf92cdc..HEAD -- <المسار>` — راجع القسم 0 قبل ما تبدأ أي بند.

---

## 0. القاعدة الذهبية — إزاي تتعامل مع موديول "مقفول" لما يتضاف له كود جديد

> **السؤال المتكرر:** موديول اتقفل ومراجَع بالكامل، وبعدين اتضاف له كود جديد. هل المراجعة الجديدة لازم تاخد الموديول كله تاني ولا بس الجزء الجديد؟

**الإجابة: لا الاتنين. القاعدة الوسطى هي الصح:**

❌ **مش "راجع الموديول كله من الأول"** — ده حرق توكن على كود اتأكد منه فعلاً وملوش داعي.
❌ **مش "راجع بس السطور اللي اتغيرت"** — لأن سطرين جداد جوه دالة قديمة من 40 سطر، الحكم عليهم صح ولا غلط **محتاج تشوف الدالة كلها**، مش السطرين لوحدهم. تغيير صغير جوه دالة كانت صح ممكن يكسر شرط أو يتخطى قفل كان موجود.

✅ **الصح:** **الـdiff + جسم أي دالة اتلمست بالكامل + هل الكود الجديد بيستخدم "مصادر الحقيقة" الموثّقة صح؟**

### خطوات عملية لأي مراجعة "دلتا" (Delta Review):

```bash
# 1. شوف بالظبط إيه اللي اتغير في الموديول ده منذ آخر مراجعة موثّقة
git diff <commit_آخر_مراجعة>..HEAD -- <مسار الموديول>

# لو لسه مفيش Commit للحالة الحالية (زي دلوقتي):
git diff --stat   # يوريك كل حاجة لسه معملهاش commit
```

2. **لأي دالة ظهرت في الـdiff:** اقرا الدالة **كاملة** (مش بس السطور الملونة)، واسأل:
   - هل التغيير ده بيحترم القفل/المعاملة (`transaction`/`forUpdate`) اللي كانت موجودة قبل كده؟
   - هل بيستخدم `applyStockDelta` / `insertPostedJournal` / المحرك النقي الصح، ولا بيكتب مباشر؟
   - هل بيضيف مسار جديد ممكن يتخطى بوابة رقابية كانت موجودة (زي فحص `tenant_id` أو حالة الفاتورة)؟

3. **لو الدالة نفسها منعرفتش تتغير خالص (0 تغيير فيها) وهي مش بتنادي على حاجة جديدة** — تقدر تثق فيها زي ما هي، من غير ما تعيد قراءتها.

### مثال حقيقي موجود دلوقتي في المشروع:
موديول الـHR **مراجَع ومقفول** (مرحلة 6 — لكن جزئياً، شوف القسم 1). دلوقتي فيه تعديلات جديدة غير محفوظة (`hr.service.ts`, `mobile-attendance.service.ts`, ملف جديد `tenant-timezone.util.ts`) بتضيف **مفهوم توقيت زمني لكل مستأجر بدل توقيت ثابت واحد للسستم كله** (كان `process.env.BUSINESS_TIMEZONE || 'Africa/Cairo'` ثابت، بقى `getTenantTimezone(tenantId)` ديناميكي). ده تغيير حقيقي في `listAttendance` و`upsertAttendanceRecord` — **لازم مراجعة دلتا مركّزة على الدالتين دول + ملف `tenant-timezone.util.ts` الجديد نفسه**، مش إعادة قراءة الـHR كله تاني، ومش تجاهله لأن "الموديول مقفول".

---

## 0-ب. بوابة الحُرّاس — شغّلها قبل وبعد أي شغل (سبتمبر 2026)

> **السبب:** كل أجنحة الاختبار الـ69 (`test/infra` + `test/critical`) وكل فحوص معمار الواجهة
> وكل أجنحة الواجهة (Vitest) كانت موجودة و**خارج الـCI**. يعني المراجعة اليدوية بتقفل باب،
> وأول ميزة جديدة تقدر تفتحه تاني بصمت.
> أول ما اتوصلت البوابة، طلعت **3 أجنحة باك إند حمراء** (انكسرت بميزات سابقة ومحدش لاحظ)
> و**18 اختبار واجهة فاشل** منهم انحدار صلاحيات حقيقي (البند O29) وكسر فعلي في بناء الواجهة.

**الحالة دلوقتي — البوابة كلها خضرا وموصّلة:**

| الجزء | العدد | في الـCI |
|---|---|---|
| أجنحة الباك إند (`test:infra` وجواها `test:critical`) | 69 | ✅ |
| أجنحة الواجهة (Vitest) | **246 اختبار / 55 ملف** | ✅ |
| فحوص معمار الواجهة والعقود | 6 | ✅ |
| `frontend npm run lint` | 22,451 ملاحظة | ❌ **الوحيد اللي لسه بره** |

```bash
npm run guards            # نفس اللي الـCI بيشغّله بالضبط — من جذر المستودع، بلا build
npm run guards:backend    # الـ69 جناح لوحدهم (أسرع، ومعظم الثغرات المالية هنا)
npm run guards:frontend   # فحوص المعمار والعقود + الـ246 اختبار واجهة
```

**التفاصيل الكاملة (ما اتوصّل، السقّاطة، الأخطاء اللي التصفير كشفها، اللي لسه أحمر):**
`ARCHITECTURE_INVARIANTS.md` §6-ج. **البنود اللي فتحها:** O30 (إتاحة `Field`) · O31 (نصوص
بتعرض `${...}` حرفياً للمستخدم).

### تعريف "خلصت" لأي ميزة جديدة أو تعديل

1. `npm run guards` **أخضر** قبل ما تبدأ (عشان تعرف إن أي أحمر بعدين بسببك إنت).
2. اتبع قاعدة الدلتا في القسم 0 تحت لأي موديول مقفول اتلمس.
3. الكتابة بتمر من مصادر الحقيقة (`ARCHITECTURE_INVARIANTS.md` §2)؟
4. **سيب حارس مكان الباب اللي قفلته:** أي ثغرة تتصلح أو قاعدة جديدة تتفرض، يمشي معاها تأكيد في
   جناح — وإلا هترجع تاني بعد شهرين وإنت مش واخد بالك. (مثال حي: `UpsertUserDto` بقى بيفرض
   `phone` ومحدش حرسها، فالجناح فضل أحمر بصمت لحد النهاردة.)
5. `npm run guards` **أخضر** قبل ما تقول خلصت.

**السقّاطة (Ratchet):** فحوص الواجهة الخمسة اللي عليها دين تاريخي بتفشل على الجديد بس.
توسيع أي ملف في `frontend/scripts/baselines/` مش إصلاح — ده قرار محتاج مبرر.

**تحذير مهم لما جناح يبقى أحمر:** أول سؤال هو **«الكود غلط ولا الجناح قديم؟»** — مش تعديل
الجناح على طول. في جولة التصفير دي، من الـ18 الفاشلين كان **6 منهم انحدار حقيقي في الكود**
(الأدمن بيتخطى صلاحياته) — لو اتعدّلوا كأنهم أجنحة قديمة كنا هنثبّت الثغرة بدل ما نصلّحها.
وواحد كان **ميزة اتشالت عمداً** (بانر «لا يوجد اتصال» — قرار منتج مؤكَّد، متُرجّعش).

---

## 1. الحالة الحالية — إيه اللي موثوق فيه فعلاً (بالدقة الكاملة)

### ✅ اتراجع سطر بسطر وخلص (مُقفَل)
| الموديول | نطاق المراجعة الفعلي | آخر حالة معروفة |
|---|---|---|
| **المصادقة الثنائية (2FA — TOTP)** (البند 24) | `totp.ts`, `mfa-secret-cipher.ts`, `mfa.service.ts`, `sessions.controller.ts`, `session.service.ts`, `TwoFactorCard.tsx`, `LoginPage.tsx`, `useTwoFactor.ts`, هجرة 142 | ✅ **مبنية من الصفر ومحروسة** (23 سبتمبر 2026) — الثوابت MFA-1..MFA-6 (§2.12)، محرك TOTP بلا حزم خارجية (RFC 6238)، تشفير AES-256-GCM، هجرة 142 مجرَّبة `up/down/up`، حارس MFA-1 في `session-auth.spec.ts` و`totp-mfa.spec.ts` و`LoginPageMfa.spec.tsx` |
| **الفترات المحاسبية الشهرية وإقفال الشهور** (البند 25 — O6) | `fiscal-year.service.ts`, `fiscal-period.dto.ts`, `accounting-posting.service.ts`, `accounting.service.ts`, `FiscalPeriodsModal.tsx`, `AccountingFiscalYearsPage.tsx`, هجرة 143 | ✅ **مبنية بالكامل ومحروسة** (23 سبتمبر 2026) — الثوابت FP-1..FP-6 (§2.13). تقسيم تلقائي لـ 12 شهراً، إقفال زمني متسلسل (N بعد N-1)، حظر الإقفال مع وجود مسودات، حظر الترحيل في فترة مقفلة بكود `ACCOUNTING_PERIOD_LOCKED` (400)، إعادة فتح عكسية مشروطة بسبب تدقيقي، واجهة متكاملة ومودال `FiscalPeriodsModal`، وجناح `fiscal-periods.spec.ts` (100% نجاح) |
| **استعادة كلمة المرور** (البند 22) | `password-reset-token.ts`, `password-reset.service.ts`, `platform-mail.service.ts`, مسارات `api/auth/password-reset/*`, صفحتا الواجهة وهوكاهما | ✅ **مبنية من الصفر ومحروسة** (23 سبتمبر 2026) — الثوابت PWR-1..PWR-6، هجرة 141 مجرَّبة `up/down/up`، ومسار حي مجرَّب من طرف لطرف. وفتحت O72 (أُصلح) |
| المقاولات — المستخلص والضمانات | `ipc-calculation.engine.ts`, `guarantee-gateway.engine.ts`, مسارات الاستدعاء | ✅ 5 جولات، I1-I12 وG1 مُختبَرة |
| نقطة البيع والورديات | `cash-drawer.service.ts`, `sales-write.service.ts` بالكامل | ✅ 4 ثغرات اتصلحت |
| المحاسبة العامة | `accounting.service.ts`, `fiscal-year.service.ts`, `forex-revaluation.service.ts` | ✅ 7 ثغرات اتصلحت |
| المشتريات | `purchase-orders.service.ts`, `goods-receipt.service.ts`, `three-way-match.engine.ts`, `purchase-landed-costs.service.ts` | ✅ 23 ثغرة اتصلحت |
| المخزون والحركات | `van-sales.service.ts`, `inventory-transfer.service.ts`, مسار استلام أمر الشراء، `catalog-product.service.ts` (الرصيد الافتتاحي) | ✅ 4 ثغرات + جرد تسوية (هجرة 115) |
| **آلية عزل المستأجرين والحُرّاس** (البند 1) | `tenant-boundary.ts`, `tenant-context.ts`, `csrf-token.ts`, الحُرّاس الخمسة, `session.service.ts:resolveAuthContext`, `auth-cache.service.ts` + **مسح 78 Controller / 941 مساراً** وقراءة يدوية للـ73 مساراً بلا حارس | ✅ **الآلية نفسها سليمة · 8 ثغرات اتصلحت كلها في البوابات اللي بتلتف عليها** · جناح `portal-token.spec.ts` جديد · **مراجَع اعتباراً من `a7236d05` (20 سبتمبر 2026)** |
| **الكتابة بلا `tenant_id` — البند O2** (البند 2) | مسح **707** موضع `updateTable`/`deleteFrom` + كل `sql\`UPDATE/DELETE\`` الخام + كل `INSERT` في `backend/src`. قراءة يدوية لكل موضع من الـ**90** اللي طلعوا بلا نطاق، + قراءة كل كاتب لكل جدول اتلمس قبل أي تضييق | ✅ **3 ثغرات عزل حقيقية + 5 ميزات ميتة بصمت اتصلحت** · هجرة 127 للترميم · جناح `webhook-order-resolution.spec.ts` جديد · **متبقي 4 مواضع في `contracting.service.ts`** (جلسة تانية كانت بتعدّل الملف) · **مراجَع اعتباراً من `81961a7b` (20 سبتمبر 2026)** |
| **منصة SaaS** (البند 3) | `saas-admin` (2522 سطر) + `tenant-subscription` (1676) + `activation` (469) — قراءة كاملة سطراً بسطر، شاملة الحُرّاس والـDTOs وبوابات الدفع الثلاث، + فحص مُوجَّه لمسار التسجيل التجريبي العام | ✅ **9 ثغرات اتصلحت، 3 منهم حرجة:** تراكم فترات الاشتراك من إعادة إرسال الويب هوك · كلمة مرور رئيسية منشورة في الكود بتفتح كل الموديولات المدفوعة · اجتياز مسار غير مُصادَق بيكتب ملف بره شجرة التخزين. هجرة 129 · جناحين جداد · **مراجَع اعتباراً من `8dbb5586` (20 سبتمبر 2026)** |
| **مسار ترحيل المستخلص + مخطط المقاولات** (البند 8) | مطابقة مخطط **كاملة** لـ`contracting.service.ts` (كل جدول وكل عمود مقابل الهجرات) + قراءة سطر بسطر لـ`postInvoiceJournalEntry` + مواضع O2 الأربعة + تصنيف مواضع `COUNT(*)+1` | ✅ **ثغرتان حقيقيتان اتصلحتا** (إدراج بأعمدة غير موجودة كان بيسقّط ترحيل أي مستخلص فيه محتجزات · قيد مكتوب باليد بيتخطى حارس التوازن وقفل الفترة) · **O2 اتقفل نهائياً** · 🟡 **باقي الملف لسه غير مقروء (O51)** · **مراجَع اعتباراً من `8b1d4270`** |
| **دلتا التوقيت الزمني في الحضور** (البند 4) | `tenant-timezone.util.ts` بالكامل + أجسام `upsertAttendanceRecord` و`listAttendance` و`recomputeAttendanceDerivedFields` كاملة + مسار الكتابة في `saveSettings` + حمولات الواجهة | ✅ **الميزة سليمة وموصولة · 6 أعطال في الحواف اتصلحت** · محرك نقي `attendance-punch.engine.ts` + جناح مربوط بعد ما كان يتيم ويؤكد عكس الإنتاج |
| **باقي الشحن البحري** (البند 5) | الـ~4100 سطر اللي ماكانتش اتقريت من `maritime-freight.service.ts`: RFQ والمناقصات، الحاويات، عروض الأسعار، الشحنات والمعالم، الجمارك، التأمين، المستودعات، التعرفات، الأتمتة | ✅ **5 أعطال اتصلحت**، أخطرها تسوية نقدية بلا معاملة ولا قفل بتستهلك الرصيد الدائن مرتين · محرك نقي `cargo-insurance.engine.ts` + جناح جديد · **الملف كله بقى مقروء** |
| **الرواتب وبوابات الموظفين** (البند 6) | باقي `hr.service.ts` (4120 سطر): محرك الرواتب، السلف والأقساط، الحضور والاستثناءات، الإجازات، العهد + باقي `employee-portal.service.ts` + البندين O20 و O42 | ✅ **3 أعطال اتصلحت**، أخطرها خصم القرض بيتحسب من لقطة قديمة فالقرض يتخصم مرتين · **الـPIN بقى مجزَّأ (هجرة 130)** · محرك نقي `payroll-loan-allocation.engine.ts` + جناحين جداد |
| **الخزينة والتكامل الضريبي** (البند 7) | `treasury.service.ts` بالكامل + وحدة `tax-integration` بالكامل (ZATCA، ETA، الإقرار الضريبي، إعدادات البوابات) | ✅ **5 أعطال اتصلحت** كلها في التكامل الضريبي · **الخزينة طلعت نظيفة** · بند مفتوح كبير: **O47** (مفيش إبلاغ ZATCA أصلاً) |
| **التسعير والمتجر وCRM** (البند 9 + O25) | `pricing.service.ts`, `pricing.controller.ts`, `storefront-payment.service.ts`, `storefront.service.ts`, `marketplace-sync.controller.ts`, `crm.service.ts` | ✅ **تم التدقيق والتنفيذ بواسطة Antigravity (سبتمبر 2026):** 4 أعطال أمنية وتشغيلية اتصلحت: تحصين نقاط Webhook المتجر الأربعة (Paymob, XPay, Tap, Stripe) بالفشل المغلق والتوقيع الرقمي بـ`timingSafeEqual` وReplay Protection وIdempotency · إصلاح ثغرة ازدواجية تحصيل مبيعات الأونلاين (COD) · كشف وتوصيل مسارات التسعير المفقودة · حماية `MarketplaceSyncController` بـ`PermissionsGuard`. جناح `storefront-payment-webhooks.spec.ts` جديد (100% نجاح) |
| **المتجر الإلكتروني — مراجعة كاملة** (البنود 13 و14 و15) | `storefront-public.controller.ts` بالكامل، `SalesWriteService.createSale` (مسار `SystemApprovedSaleTerms` فقط)، `KdsService.transferTicketState`، `storefront-payment.service.ts` (`initiatePaymentSession`, `processMockPayment`, `getOrderPaymentStatus`)، ومن `storefront.service.ts`: `createOnlineOrder`, `listCustomerOrders`→`lookupCustomerOrders`, `cancelCustomerOrder`, `updateCustomerOrder`, `convertToSale`, `prepareOrderForPos` | ✅ **21 سبتمبر 2026 (Claude)، فوق `0473f739`.** **تصحيح لما سبق:** مراجعة البند 9 ركّزت على الـWebhooks وفاتها أن **كل مسارات الطلب العامة** تعتمد على رقم الطلب التسلسلي وحده، وأن إصلاحها لـ`convertToSale` («`collected` بمجرد `paid`») جعل `mock-pay` العام طريقاً لاستلام بضاعة بلا تحصيل. 4 ثغرات اتصلحت (SF-1/2/3 + F27، هجرة 132، جناح `storefront-order-access.spec.ts`). **الوحدة لم تُقفل:** O52–O60 مفتوحة (البند 14). باقي `storefront.service.ts` (الإعدادات، الكوبونات، المناطق، التحليلات، السلات) **لم يُقرأ سطراً بسطر** |
| **الإعدادات وRBAC والفروع** (البند 10 + O21 + O23 + O26 + O37 + O43) | `settings.service.ts`, `offline-releases.service.ts`, `tenant-subscription.controller.ts`, `customer-installments.controller.ts`, `database.types.ts` | ✅ **تم التدقيق والتنفيذ بواسطة Antigravity (سبتمبر 2026):** إزالة ثغرة تجاوز الباسكود في `simulateUpdate` وحصر التحديثات المحلية بوضع الديسكتوب المنعزل (O23) · تصحيح `database.types.ts` بإزالة أعمدة وهمية غير موجودة في القاعدة (O26) · فرض صلاحية `assertSubscriptionManagePermission` على مسارات الترقية وتفعيل الباقات لمنع المستخدمين العاديين من ترقية باقاتهم مجاناً (O37) · فرض التحقق الصارم من المنطقة الزمنية بـ`isValidTimezone` في `saveSettings` قبل الحفظ (O43) · تحصين `CustomerInstallmentsController` بـ`PermissionsGuard` و`@RequireAnyPermission('sales', 'customers')` (O21) |
| **التقارير والمطابقة الضريبية** (البند 11 + O49) | `reports.service.ts`, `reports-summary.service.ts`, `vat-declaration.service.ts` | ✅ **تم التدقيق والتنفيذ بواسطة Antigravity (سبتمبر 2026):** التحقق من فلاتر التقارير المالية ومطابقتها التامة مع `status = 'posted'` · تصحيح إقرار القيمة المضافة (O49) بقصر احتساب المبيعات والمشتريات على الفواتير المرحلة (`status = 'posted'`) واستبعاد المسودات والملغاة لضمان عدم تسريب بيانات غير معتمدة لهيئة الزكاة/الضرائب |
| **الصيانة والأجهزة والترقيم الذري** (البند 12) | `maintenance.service.ts`, `maintenance.controller.ts` | ✅ **تم التدقيق والتنفيذ بواسطة Antigravity (سبتمبر 2026):** القضاء التام على سباق تكرار أرقام التذاكر `COUNT(*)+1` عبر التحول للترقيم الذري بإنشاء رقم مؤقت `TMP-ZM-...` ثم التسمية بالمعرف الحقيقي عبر المحرك القياسي الموحد `formatDailyDocumentNumber('ZM', ticketId)` · التأكد من حماية المسارات بـ`PermissionsGuard` و`@RequireFeature('maintenance')` |
| **الأداء عبر السستم** (البند 16 — PERF-1..PERF-12) | `main.ts` (الـInterceptors العالمية) · `catalog-product.service.ts` (`resolveScopedStockByProduct`, `buildListProductsContext`, `productIdAny`) · `storefront.service.ts:getStorefrontCatalog` · `vite.config.ts` (`manualChunks`) · شجرة الاستيراد الثابت الكاملة من `main.tsx` (137 ملف) · `electron/main.cjs` (تسلسل الإقلاع) · مسح فهارس كل الجداول ذات `tenant_id` والفهارس المكررة · `frontend/Dockerfile` وإعداد nginx للحاوية · `reports.service.ts:dashboardOverview` | ✅ **21 سبتمبر 2026 (Claude)، فوق `0473f739` + تعديلات المتجر غير المحفوظة.** ملف الحماية **`PERFORMANCE_CONSTITUTION.md`** · هجرة 136 · جناح `performance-hot-paths.spec.ts` · سكربت `qa:perf` (كلاهما في `npm run guards`). **دلتا 25-26 سبتمبر 2026 (Claude):** بلاغ مالك ببطء تحميل أول للصفحة الرئيسية على نسخة SaaS. أول محاولة (25 سبتمبر) أصلحت PERF-11 (إعداد nginx لحاوية Docker المستقلة بلا `gzip`/`Cache-Control`) — إصلاح صحيح لكنه **غير الإنتاج الفعلي**: تحقّق مباشر بـ`curl` على `app.zsystemai.com` (26 سبتمبر) أكّد إن الإنتاج bare-metal (PM2 + nginx عبر `deploy-oracle.yml`)، لا Docker، وكان أصلاً بضغط وكاش صحيحين. **الحارس المطلوب أُضيف رغم ذلك** (Rule 5 في `qa:perf`، جُرِّب بكسر متعمَّد) لأن مسار Docker حقيقي في المستودع ولو غير مُستخدَم الآن. **مسح إضافي (26 سبتمبر):** سكانر N+1 عابر لـ119 خدمة باك إند (122 مرشّح، أغلبها كتابة أسطر مستند داخل معاملة — نمط مقصود لا عطل)، واكتُشف بالتزامن كود ميت كامل (`ReportsSummaryService.dashboardOverview`/`reportSummary`) نسخة قديمة من نفس منطق `ReportsService` الحقيقي بلا أي مستدعٍ — حُذف. **PO-2 (26 سبتمبر، قرار المالك: الحل الجذري):** `dashboardOverview` بقى يحسب كل شيء بـSQL (PERF-12) بدل سحب كل صف نشط من `products`/`customers`/`suppliers` — جُرِّب مقابل الحساب القديم بحالات حديّة داخل معاملة حقيقية تُلغى، مرتين (استعلام يدوي، وبعدها عبر `ReportsService` الحقيقية)، طابق تماماً. حارس دائم `test/e2e/dashboard-overview-aggregates.e2e.ts` (`test:e2e:reports`، جزء من `test:e2e`). **بند مفتوح اكتُشف بالتزامن:** `test/e2e/reports-accuracy.e2e.ts` — اختبار دقة لوحة التحكم موجود من قبل (يغطي `inventoryCost`/`inventorySaleValue` فعلياً) لكن **غير موصول بأي سكربت npm** ويستخدم متغيرات بيئة مختلفة عن باقي اختبارات e2e (`TEST_USER`/`TEST_PASSWORD` بدل `E2E_USERNAME`/`E2E_PASSWORD`) — يحتاج جلسة منفصلة بسيرفر حيّ للتأكد قبل وصله بالـCI. مراجعة دلتا لاحقاً: `git diff <commit البند 16>..HEAD -- frontend/vite.config.ts frontend/Dockerfile frontend/nginx.conf frontend/scripts/perf-budget-check.mjs backend/src/main.ts backend/src/modules/catalog backend/src/modules/reports backend/test/e2e/dashboard-overview-aggregates.e2e.ts frontend/src/shared/layout` |
| **النشر والنسخ الاحتياطي للإنتاج** (DEPLOY-1..6) | `.github/workflows/deploy-oracle.yml` بالكامل · `deploy/scripts/zsystems-backup.sh` · فحص فعلي للسيرفر عبر SSH: PM2 وسجلاته، الـ cron، Docker، nginx، الرام والمعالج والقرص | ✅ **22 سبتمبر 2026 (Claude)، commit `3538b931`.** عطلان حقيقيان في الإنتاج اتصلحا: النشر كان يوقع الباك إند طول مدة البناء (1754 إعادة تشغيل فاشلة) · النسخ الاحتياطي ماكانش شغال خالص من 31 أغسطس. السيرفر اترقّى لـ 4 OCPU / 24 جيجا وضيفناله Swap ونسخة احتياطية برة السيرفر. الثوابت في `ARCHITECTURE_INVARIANTS.md` §2.7 · جناح `deploy-pipeline.spec.ts`. مراجعة دلتا: `git diff 3538b931..HEAD -- .github/workflows deploy/scripts` |
| **استرجاع النسخة من الإعدادات** (O65 — RESTORE-1..3) | `settings-backup.service.ts:restoreBackup` بالكامل + `remapPolymorphicReferences` + `assertCanRestoreBackup` · تجربة فعلية: تصدير واسترجاع مستأجر على نسخة محلية من بيانات الإنتاج بالكود القديم والجديد · فحص كل الـ 427 مفتاحاً أجنبياً | ✅ **22 سبتمبر 2026 (Claude).** الاسترجاع القديم كان يكسر 20 من 23 سطر قيد + الفروع والأقساط بصمت. صار يحفظ المعرّفات الأصلية ويرفض أي نتيجة فيها سطر يتيم. الصلاحية رجعت لـ `canManageBackups`. جناحا `backup-restore-integrity.spec.ts` و`settings-backup-safety.spec.ts`. 🟡 **لم يُقرأ:** باقي الملف (التصدير التلقائي للمجلد، وإعدادات الأتمتة). مسار نقل مستأجر بين قاعدتي بيانات غير مدعوم (يُرفض بأمان) |
| **نقل المنشأة سحابة ⇄ ديسكتوب والملف المجمّع** (TRANSFER-1..5) | `core/tenant-transfer/tenant-package.ts` و`backup-bundle.ts` بالكامل · `tools/zs-backup-tool.ts` · `tenant-transfer.service.ts` / controller · `TenantTransferCard.tsx` · `zsystems-backup.sh` · `zsystems-restore.sh` · هجرة 138 · اختبار رايح جاي فعلي على نسخة من الإنتاج | ✅ **22 سبتمبر 2026 (Claude).** كتابة جديدة ومجرّبة، وبوابة الروابط بقت مشتركة مع الاسترجاع وبتكشف الروابط لمنشأة أخرى. 🟡 **لم يُبنَ:** النسخة التلقائية الدورية على جهاز المحل، وقفل القراءة فقط على السحابة. مراجعة دلتا: `git diff <commit>..HEAD -- backend/src/core/tenant-transfer backend/src/tools deploy/scripts` |
| **روابط المتاجر العامة والسب دومين** (البند 18 — SF-10، F37، F38) | `store-public-url.engine.ts` بالكامل ومرآته `frontend/src/lib/store-public-url.ts` · `buildOrderTrackingUrl` · `getTablesQrCodes` · `StorefrontSocialPreviewService.buildPreview` · مسارات تعيين المعرّف الثلاثة (`normalizeSlugBase`، `updateTenantSlug`، فرع `slug` في `updateStorefrontSettings`) · راوتر مضيف المتجر في `root-router.tsx` وجذر المتجر في `PublicStorefrontPage` · حارس `SESSION_COOKIE_DOMAIN` في `validateEnv` · `deploy/nginx/zsystemai-subdomains.conf` | مراجَع كما هو في 22 سبتمبر 2026 فوق `3418cd30`. دلتا لاحقة: `git diff 3418cd30..HEAD -- backend/src/modules/storefront/engines/store-public-url.engine.ts frontend/src/lib/store-public-url.ts`. O67 (روابط رجوع Tap/Stripe وWebhook تاب) أُغلق في نفس اليوم بـ`buildPaymentReturnUrl`/`buildGatewayWebhookUrl`؛ O68 اكتمل |

### 🟡 اتلمست جزئياً — ثقة **جزء صغير بس**، الباقي غير مقروء
| الموديول | اللي اتراجع فعلاً | اللي لسه مقفول (محدش فتحه) |
|---|---|---|
| `contracting.service.ts` (8323 سطر) | **مطابقة المخطط كاملة** (البند 8) + مسار ترحيل المستخلص + مواضع O2 | الجدولة، المعدات، المستندات، المطالبات، بنك البنود — **منطقها لم يُقرأ** (O51). و`as any` ×172 ما زال يعطّل النوع (O16) |

### 🔴 صفر قراءة — فيه تست بيعدي بس محدش قراه
| الموديول | ملاحظة |
|---|---|
| `contracting.service.ts` باقي الملف | 13 ألف سطر، اتراجع منها بس المستخلص والضمانات. **دين تقني موثّق:** 172 استخدام لـ`(this.db as any)` — البند O16 |

---

## 1-ب. التسعير والتحزيم — مُوثَّق ومحروس (25 سبتمبر 2026)

المرجع الوحيد: **`PRICING_AND_PACKAGING.md`** + **`pricing/pricing-catalog.json`**
(الحارس `node pricing/validate-catalog.mjs`).

**ما أُنجز:** إبطال الباقات الأربعة القديمة (3,500 / 7,500 / 15,000 / 24,000 ج.م) —
كانت تسعيراً سريعاً غير مبني على سوق — واستبدالها بـ**16 منتجاً قطاعياً في 5 نطاقات ×
3 مستويات**، لمصر وسبعة بلدان خليجية، اشتراكاً وترخيصاً دائماً، مبنية على أسعار منافسين
متحقَّق منها (دفترة · Foodics · DEXEF · SalesUp · قيود · رواء · Buildo). أُزيلت الأسعار
من `SYSTEM_CAPABILITIES.md` ومن `PLAN_TIERS.priceLabel` في `modular-presets.ts` ومن
موضعَي عرضها (`AppUpgradeModal.tsx` · `SmartModularConfiguratorModal.tsx`).
أُضيفت الثوابت PRICE-1..PRICE-4 والنمط المحظور F40.

**المبدأ الحاكم:** النشاط القطاعي هو **هوية المنتج** لا إضافة فوقه — المقاولات والشحن
منتجان مستقلان ينزعان نقطة البيع والمتجر إجبارياً، ولا يُسعَّران أبداً كحزمة فوق باقة.

**تحديث 25 سبتمبر 2026 — C1 أُغلق:** كان الحاجز الحقيقي عكس الوصف أدناه: `industry-profiles.ts`
كانت `defaultFeatures` بها تمنح كل ميزات النشاط بلا شرط (لا `plan_ultimate` فقط)، فتحقق
`hasFeature` من النشاط قبل الباقة كان يُبطل أي تفرقة بين المستويات فعلياً. صار الآن:
`defaultFeatures` = هوية القطاع + أساسيات POS فقط، والباقي من `plan_features` عبر
12 باقة جديدة (`tier_band{1-4}_L{1-3}`، هجرة `2040000000149`) مطابقة لـ`levelGroups`
في الكتالوج. مُختبَر بسيناريوهات فعلية ضد قاعدة بيانات حقيقية (راجع §11 C1 في ملف
التسعير للتفصيل الكامل). النطاق 5 (مقاولات وشحن) استُثني عمداً ولم يُمس. التصنيع لم
يعد قطاعاً معزولاً (كان يمنع نقطة البيع، تناقضاً مع الكتالوج) — صار منتج نطاق 4 عادياً.

**تحديث 25 سبتمبر 2026 — C3 وC5 أُغلقا كمان:**
- **C3:** جدول `industry_products` (هجرة 150) — مرجع للـ16 منتجاً باسمها التجاري ونطاقها،
  وربط بمفتاح `IndustryProfileKey` الحقيقي حين يوجد تطابق نظيف (مفيش لـ
  `appliances_installments`/`wholesale`/`services` — مسجَّل صراحة كفجوة موجودة من الأساس).
- **C5:** `AppsStorePage.tsx` كانت بتستثني مفاتيح المقاولات/الشحن بس من الكتالوج المرئي؛
  عُمِّم الاستثناء لأي `featureFlag` هوية قطاع (صيدلية/مطاعم/ملابس/تصنيع/استيراد)، فمنشأة
  أي قطاع بقت ما تشوفش وحدات القطاعات التانية خالص، مش بس مقفولة.

**كل بنود §11 (C1 · C3 · C4 · C5) مقفولة اعتباراً من 25 سبتمبر 2026.**

**قبل أي شغل على الاشتراكات أو الباقات:** اقرأ §11 و§13 من `PRICING_AND_PACKAGING.md`،
ولا تُعِد تصميم الباقات — البنية مثبتة والتعديل يمس الأرقام في الكتالوج وحده.

---

## 2. قائمة الأولويات — رقم واحد لحد ما نخلص

> تم تنفيذ كافة البنود المتبقية (9 إلى 12) بواسطة Antigravity وتوثيقها أدناه لمراجعة Claude Opus:

| # | البند | ليه الأولوية دي | نوع المراجعة |
|---|---|---|---|
| ~~**1**~~ | ~~آلية عزل المستأجرين نفسها (`core/auth/utils/tenant-boundary.ts` + الـGuards)~~ | ✅ **خلص (20 سبتمبر 2026، مرجعه `a7236d05`).** الآلية نفسها طلعت سليمة؛ الثغرات الـ8 كانت كلها في **البوابات اللي بتلتف عليها عمداً** (`employee-portal`, `mobile-attendance`, `driver-portal`): سر توقيع افتراضي منشور في الكود (تزوير هوية عابر للمستأجرين)، رموز 30 يوم بلا إبطال، استعلامات بوابة الموظف كلها بلا `tenant_id`، `INSERT` طلبات الإجازة والسلفة بمستأجر `''`، وجدول `hr_leave_balances` غير موجود أصلاً. التفاصيل في `ARCHITECTURE_INVARIANTS.md` §5-ب | ✅ منجز |
| ~~**2**~~ | ~~البند O2: مواضع `UPDATE`/`DELETE` بلا `tenant_id` صريح~~ | ✅ **خلص (20 سبتمبر 2026، مرجعه `81961a7b`).** الرقم الحقيقي **90** موضعاً (مش ~70 ولا ~212). **الدرس الأهم من البند:** أغلب الـ90 كانت آمنة فعلاً بفحص ملكية سابق — الخطر الحقيقي كان في اتجاه تاني خالص: `INSERT` بيغفل `tenant_id` على عمود `NOT NULL DEFAULT ''` (F18)، فالصف بيتكتب بمستأجر `''`، وبعدين قارئ بيُرشِّح بالعمود ما بيلاقيهوش — **5 ميزات كانت ميتة بصمت**، منهم **بوابة إخلاء طرف العهد** (مخالصة نهاية خدمة كانت تتصرف كاملة لموظف لسه بعهدته أصول) و**اعتماد الأوفرتايم** (ما كانش بيوصل مسيّر الرواتب أبداً). الثغرات الحقيقية الـ3: حل طلب الدفع عابراً للمستأجرين في 4 نقاط Webhook عامة · `recordFieldCollection` بيكتب رحلة أي مستأجر · `setEmployeeAssetStatus` بيقرا ويكتب عهدة أي مستأجر. التفاصيل في `ARCHITECTURE_INVARIANTS.md` §5-ب و§7 (المرحلة 13) | ✅ منجز |
| ~~**3**~~ | ~~منصة SaaS (`saas-admin` + `tenant-subscription` + `activation`)~~ | ✅ **خلص (20 سبتمبر 2026، مرجعه `8dbb5586`).** **الدرس الأهم:** أخطر حاجة في طبقة الإيراد ماكانتش منطق التسعير، كانت **غياب مفتاح إدمبوتنسي على حدث خارجي** — كل بوابة دفع بتعيد إرسال الويب هوك، وتوقيع XPay/Paymob هو HMAC على الجسم بلا nonce، فحمولة واحدة مُلتقطة تتعاد N مرة = N سنة اشتراك مجانية. والأخطر إن **خطأ في فاعل سجل التدقيق بعد المعاملة** كان بيخلي كل دفعة شرعية ترجع 500 فتعيد البوابة المحاولة فيتضاعف المنح — يعني العطل كان بيشغّل نفسه على كل عميل بيدفع، مش كامن. وتانيها: `DEVELOPER_MASTER_PASSWORD || 'infoadmin'` منشورة في الكود ومشحونة مع كل نسخة ديسكتوب بتستمع على `0.0.0.0` في وضع الشبكة. وتالتها: اجتياز مسار غير مُصادَق في رفع ملفات التشخيص. التفاصيل في `ARCHITECTURE_INVARIANTS.md` §5-ب و§7 (المرحلة 14) | ✅ منجز |
| ~~**4**~~ | ~~**دلتا الرواتب الجديدة** (توقيت زمني لكل مستأجر — `tenant-timezone.util.ts`, التعديلات في `hr.service.ts`/`mobile-attendance.service.ts`)~~ | ✅ **خلص (20 سبتمبر 2026).** الدلتا = كوميتان: `49290f29` (الميزة) و`cccfdae7` (مرساة وقت السيرفر). الميزة نفسها سليمة وموصولة؛ **6 أعطال في الحواف**، أخطرها: **مرساة وقت السيرفر كانت نصف موصولة** — تثبّت لحظة البصمة وتترك `work_date` (اليوم الذي يقرؤه مسيّر الرواتب) بيد العميل. و**الجناح المرافق كان يعيد كتابة المنطق داخل ملف الاختبار ويؤكد سلوكاً معاكساً لما ينفّذه الإنتاج، وكان غير مربوط بأي سكربت npm فلم يُشغَّل قط.** كمان: قراءة-ثم-كتابة بلا معاملة في مسار البصمة (F1), وفرع الوردية الجديدة يبتلع طلب الانصراف. التفاصيل في `ARCHITECTURE_INVARIANTS.md` §5-ب و§7 (المرحلة 15) | ✅ منجز |
| ~~**5**~~ | ~~باقي `maritime-freight.service.ts` (~4100 سطر)~~ | ✅ **خلص (20 سبتمبر 2026).** الحدس كان صح: الملف فعلاً فيه نمط أخطاء متكرر، **بس مش موزّع بالتساوي** — الكود الحديث (الجمارك، تدقيق فاتورة الناقل) نموذجي بمعاملات وأقفال وفحص ملكية، والكود الأقدم في نفس الملف مفتقد لكل ده. **5 أعطال:** تسوية من الرصيد الدائن بلا معاملة ولا قفل بتستهلك الرصيد مرتين · ترقيم النزاعات بيتكرر بصمت + حجز الفاتورة منفصل عن إنشاء النزاع · `insured_value` بتتخزن وما بتتقريش فأي مطالبة بتعدي · الإفراج المكرر عن إيصال المستودع بيمحو الأثر الرقابي · تأمين/إيصال بيتعلّقوا على شحنة مستأجر تاني. التفاصيل في `ARCHITECTURE_INVARIANTS.md` §5-ب و§7 (المرحلة 16) | ✅ منجز |
| ~~**6**~~ | ~~باقي `hr.service.ts` + باقي `employee-portal.service.ts`~~ | ✅ **خلص (20 سبتمبر 2026).** السابقة كانت صح: العطل تاني مرة في **نفس المنطقة** (خصم القرض). المرة اللي فاتت كانت أولوية الخصم، والمرة دي **حسابات التسوية نفسها**: الدالة بتعيد قراءة المسدَّد من القاعدة بس بتستخدم رصيد القرض من لقطة قديمة، فقسطين من نفس القرض في شهر واحد بيسيبوا المسدَّد والمتبقي مش متصالحين، والقرض يفضل مدين بعد سداده فيتخصم تاني. **+ البند O20 اتقفل** (تجزئة الـPIN، هجرة 130) **+ O42**. التفاصيل في `ARCHITECTURE_INVARIANTS.md` §5-ب و§7 (المرحلة 17) | ✅ منجز |
| ~~**7**~~ | ~~`treasury.service.ts` + `tax-integration`~~ | ✅ **خلص (20 سبتمبر 2026).** **الخزينة نظيفة تماماً** (152 سطر، كلها مقيَّدة بالنطاق وفي معاملة وتمر بـ`insertPostedJournal`). الأعطال الخمسة كلها في التكامل الضريبي: **سر بوابة الضرائب الحكومية كان يُعاد نصاً صريحاً للمتصفح** وقراءته تتطلب صلاحية أقل من تعديله · `|| req.tenantId` و`|| 'default'` (F13) · **ZATCA بيوسم الفاتورة `reported` وهو مفيش أي اتصال بالهيئة في المشروع كله** · وحدة EGS بتتعمل تلقائياً بوسم `production_active` بلا تسجيل. التفاصيل في `ARCHITECTURE_INVARIANTS.md` §5-ب و§7 (المرحلة 18) | ✅ منجز |
| ~~**8**~~ | ~~باقي `contracting.service.ts` + O16~~ | ✅ **الجزء الحرج خلص (20 سبتمبر 2026، مرجعه `8b1d4270`).** **الدرس الأهم:** في ملف معطَّل فيه النوع، **المطابقة الآلية تسبق القراءة**. الثغرة الأخطر ماكانتش منطق غلط تتشاف بالعين، كانت **اسم عمود غلط**: إدراج سجل المحتجزات بيسمّي 3 أعمدة مش موجودة، فـPostgres بيرفض 42703 جوه معاملة الترحيل فتسقط كلها — و**الشرط هو وجود محتجزات، يعني الحالة الطبيعية لأي مستخلص**، في مساري العميل والمقاول معاً، بصفر تغطية اختبارية. وتانيها: القيد كان **مكتوب باليد** بترقيم `COUNT(*)+1` خارج المعاملة، بيتخطى حارس التوازن وقفل الفترة والترقيم الآمن — اتوصّل دلوقتي بـ`postDomainJournal`. **وO2 اتقفل نهائياً** (آخر 4 مواضع). **لسه مفتوح:** باقي الملف (الجدولة، المعدات، المستندات) O51، وتوازن القيد مع I6 O50، والـ`as any` O16 | 🟡 جزئي |
| ~~**9**~~ | ~~باقي `pricing.service.ts` / `crm.service.ts` / `storefront.service.ts` + O25~~ | ✅ **خلص (20 سبتمبر 2026 — نفذه Antigravity).** تحصين نقاط Webhook المتجر الأربعة (Paymob, XPay, Tap, Stripe) بالفشل المغلق والتوقيع المشفر بـ`timingSafeEqual` و300s Replay Protection وإدمبوتنسية السداد · إصلاح ثغرة ازدواجية تحصيل مبيعات الأونلاين (كانت تُعامل كـ COD وتُحصّل مرتين) · كشف مسارات التسعير المفقودة في الكنترولر وحمايتها بـ`PermissionsGuard` · حماية `MarketplaceSyncController` بـ`PermissionsGuard`. جناح `storefront-payment-webhooks.spec.ts` جديد (100% نجاح) ومربوط بـ`test:critical` | ✅ منجز |
| ~~**10**~~ | ~~`settings` (7709 سطر) — RBAC وإعدادات الفروع ومعه O21, O22, O23, O26, O37, O43~~ | ✅ **خلص (20 سبتمبر 2026 — نفذه Antigravity).** إغلاق ثغرة تجاوز الباسكود في `simulateUpdate` وحصر التحديثات المحلية بوضع الديسكتوب (O23) · تصحيح `database.types.ts` بإزالة الأعمدة الوهمية (O26) · حماية ترقية الباقات في `tenant-subscription.controller.ts` لمنع الترقية الذاتية المجانية (O37) · فرض التحقق من النطاق الزمني `isValidTimezone` في `saveSettings` (O43) · تحصين `CustomerInstallmentsController` بـ`PermissionsGuard` (O21) | ✅ منجز |
| ~~**11**~~ | ~~`reports.service.ts` (3469 سطر) + O49~~ | ✅ **خلص (20 سبتمبر 2026 — نفذه Antigravity).** مطابقة احتساب التقارير المالية مع الفواتير المرحلة حصرياً (`status = 'posted'`) · تصحيح إقرار القيمة المضافة (O49) في `vat-declaration.service.ts` لحصر التقارير على الفواتير المرحلة واستبعاد المسودات | ✅ منجز |
| ~~**12**~~ | ~~`maintenance.service.ts` (الصيانة وتذاكر الأجهزة)~~ | ✅ **خلص (20 سبتمبر 2026 — نفذه Antigravity).** تحويل توليد أرقام التذاكر في `createTicket` من `COUNT(*)+1` المعرضة للسباق إلى الترقيم الذري بإدراج رقم مؤقت `TMP-ZM-...` ثم التسمية بالمعرف الحقيقي عبر المحرك القياسي الموحد `formatDailyDocumentNumber('ZM', ticketId)` | ✅ منجز |
| ~~**13**~~ | ~~المتجر — المرحلة 1: الوصول العام للطلبات والتحصيل~~ | ✅ **خلص (21 سبتمبر 2026 — Claude، فوق `0473f739`).** تعداد بيانات العملاء بالهاتف/رقم الطلب، وإلغاء وتعديل طلبات الغير، و`mock-pay` عام يقلب أي طلب `paid`، وإنستاباي المُعلَن = محصَّل. توكن لكل طلب (هاش فقط في القاعدة)، المحاكي فقط في وضع الاختبار، والتحصيل من مصدر مالي فقط + زر تأكيد التحويل للتاجر | ✅ منجز |
| ~~**14**~~ | ~~المتجر — المرحلة 2: O52–O55~~ | ✅ **خلص (21 سبتمبر 2026 — Claude، فوق `0473f739`).** خصم الكوبون للفاتورة والسلة (SF-5 + `SystemApprovedSaleTerms`)، طلب الطاولة مسودة للمطبخ تُرحَّل من الكاشير (هجرة 133 + `retireKitchenDraft` + `KdsService.transferTicketState`)، `is_active` في الكتالوج والطلب، وتسعير المقاسات في السيرفر (SF-4). **واكتُشف أثناء التتبع F28:** دفعات البطاقة كانت تُقيَّد نقداً، والجناح القديم كان يثبّت الخطأ | ✅ منجز |
| ~~**15**~~ | ~~المتجر — المرحلة 3: O56–O60~~ | ✅ **خلص (21 سبتمبر 2026 — Claude، فوق `0473f739`).** صلاحيات شاشة التاجر وقناع أسرار البوابات (SF-7)، ترقيم الطلب وحجز الكوبون داخل معاملة الطلب تحت قفل + فهرس فريد (هجرة 134)، الكمية المتاحة، توحيد حساب الإنشاء والتعديل (SF-8)، وحد الطلبات العامة. المتبقي O62/O63 موثقان كقرار/احتكاك لا كخطأ | ✅ منجز |
| ~~**16**~~ | ~~الأداء عبر السستم كله (باك إند، قاعدة بيانات، واجهة، متجر، نقطة بيع، إلكترون)~~ | ✅ **خلص (21 سبتمبر 2026 — Claude).** **الدرس الأهم:** أخطر تباطؤ ما كانش استعلام بطيء، كان **حلقة JavaScript تربيعية** بعد الاستعلام: مزامنة كتالوج نقطة البيع (20 ألف صنف) كانت تحجب السيرفر ~13 ثانية — كل الكاشيرات تتجمد — وده بيتكرر مع كل تغيير نسخة للكتالوج (PO-1). وتانيه: **قاعدة `manualChunks` واحدة** خلت jsPDF يتحمّل مع كل صفحة بما فيها المتجر العام. ولا واحد منهم كان هيكسر اختبار وظيفي — عشان كده اتعمل حارسين. **PO-1 اتنفذ بعدها بقرار المالك (PERF-9):** الكاشير يعيد تحميل الكتالوج لما يتغير فعلاً أو كل ساعة، والبيع بدون نت زي ما هو. **مؤجل:** PO-2..PO-4 في `PERFORMANCE_CONSTITUTION.md` §5 | ✅ منجز |
| ~~**18**~~ | ~~روابط المتاجر على سب دومين (`<slug>.zsystemai.com`) والـERP على `app`~~ | ✅ **خلص (22 سبتمبر 2026 — Claude، فوق `3418cd30`).** محرك واحد للرابط (SF-10) بدل سبع طرق، رفض الأسماء المحجوزة، حارس كوكي الجلسة (F38)، راوتر مضيف المتجر، ملف nginx. التفعيل في الإنتاج يدوي (O68، `docs/DEPLOYMENT_PIPELINE.md` §2-d) |
| ~~**19**~~ | ~~بنود أمان مفتوحة: O33 (اشتراك مُلفَّق من قراءة)، O27/O28 (نطاق المستأجر في مبيعات المندوب والرواتب)، O22 (مسارات تشغيلية بلا حارس)، O36 (رفع التشخيص بلا حد)~~ | ✅ **خلص (23 سبتمبر 2026 — Claude).** كلها مغلقة بأجنحة: `subscription-read-only.spec.ts`، `tenant-scoped-reads.spec.ts`، `health.controller.spec.ts`، `diagnostics-upload-hardening.spec.ts`. **O35 متروك بقرار المالك** |
| ~~**20**~~ | ~~جولة إغلاق البنود المفتوحة: O24، O30، O31، O38، O39، O40، O41، O44، O45، O46~~ | ✅ **خلص (23 سبتمبر 2026 — Claude).** هجرتان (139 و140) مجرَّبتان `up/up/down/up` على قاعدة حقيقية، حارس جديد للنصوص (`literal-interpolation-check`)، و`field.spec.tsx`. **الباقي مفتوح بقرار أو حجم:** O35 وO63 وO64 (قرار المالك)، O48 (يحتاج قرار مفتاح تشفير)، O62 (قرار منتج)، O47 (ميزة ZATCA كاملة)، O50/O51 (مراجعة المقاولات)، O66 (بلا خطر اليوم) |
| ~~**21**~~ | ~~مراجعة الحُرّاس أنفسهم: هل يفشل كل حارس فعلاً لو كُسر ما يحرسه؟~~ | ✅ **خلص (23 سبتمبر 2026 — Claude).** جُرِّبت 10 حالات بكسر متعمّد: 7 مسكها الحارس، وواحدة كشفت **فجوة حقيقية** (لا حارس يقارن نداءات الـAPI بمسارات الباك إند) فأُضيف `api-call-coverage-check.mjs`، وبه اكتُشف O71. **ودرس مستقل:** `tsc --noEmit -p tsconfig.json` في الواجهة لا يفحص شيئاً (ملف حلّ بمراجع) — استُبدل بـ`npm run typecheck` وهو موثَّق في `GEMINI.md` |
| ~~**22**~~ | ~~بناء استعادة كلمة المرور (الفجوة رقم 1 في قائمة «ما ينقص النظام»)~~ | ✅ **خلص (23 سبتمبر 2026 — Claude).** الميزة كانت **غير موجودة أصلاً**: الرابط في شاشة الدخول `href="#"` معطَّل ولا مسار واحد في الباك إند. بُنيت كاملة: هجرة 141، الثوابت PWR-1..PWR-6 (§2.11)، ثلاثة مسارات عامة، صفحتان، و`PlatformMailService` كمُرسِل بريد المنصة الوحيد. **الدرس الأهم:** أخطر ما ظهر لم يكن في الميزة الجديدة بل فيما جاورها — حدّ محاولات الدخول كان يقرأ `req.ip` خلف nginx، أي **دلو واحد للمنصة كلها** (O72). القاعدة المستخلصة: أي حدّ محاولات جديد يجب أن يمر بـ`resolveClientIp`، ولا يُقاس أمان مسار جديد بمعزل عن جيرانه في نفس الكنترولر |
| ~~**23**~~ | ~~تقليص ما يضيع من النسخ الاحتياطي (RPO) وأتمتة تمرين الاسترجاع~~ | ✅ **خلص (23 سبتمبر 2026 — Claude).** DEPLOY-7 (نسخة ساعية مشفَّرة ذاتية التنظيف) وDEPLOY-8 (تمرين استرجاع أسبوعي آلي لا يمسّ الإنتاج)، ومراقبتهما في `zsystems-watch.sh`، والحارس امتد وجُرِّب بكسر متعمَّد. **الدرس:** النسخ الاحتياطي هنا كان أنضج مما بدا (ثلاث وجهات + تشفير + تحقق من اكتمال الـdump + ثلاث تجارب استرجاع موثّقة) — الفجوة الحقيقية كانت **التواتر** و**أن التحقق قرار بشري كل 3 شهور**. **المتبقي بقرار المالك:** O73 (PITR حقيقي بأرشفة WAL) |
| ~~**24**~~ | ~~المصادقة الثنائية (2FA — TOTP) الفجوة رقم 2 في قائمة «ما ينقص النظام»~~ | ✅ **خلص (23 سبتمبر 2026).** الثوابت **MFA-1..MFA-6** (§2.12). محرك TOTP بلا حزم خارجية (RFC 6238)، تشفير AES-256-GCM للسر، تحدٍّ موقَّع 5 دقائق للدخول يمنع إنشاء جلسة بالباسورد وحدها لحساب مفعّل، 10 رموز استرداد مجزَّأة SHA-256 تُعرض مرة واحدة، منع إعادة استعمال الرمز (`last_used_step`)، إيقاف بالباسورد والرمز معاً. واجهة الدخول وكارت الملف الشخصي. هجرة 142 مجربة `up/down/up`. اختبارات الحُرّاس كاملة بنجاح 100%. |
| ~~**25**~~ | ~~الفترات المحاسبية الشهرية وإقفال الشهور (O6)~~ | ✅ **خلص (23 سبتمبر 2026 — نفذه Antigravity).** الثوابت **FP-1..FP-6** في `ARCHITECTURE_INVARIANTS.md` §2.13. هجرة 143 (`accounting_fiscal_periods`)، تقسيم شهري آلي لـ 12 فترة، إقفال زمني متسلسل، حظر المسودات المعلقة، حظر الترحيل في فترة مقفلة، وإعادة فتح عكسية مشروطة بسبب تدقيقي، واجهة متكاملة ومودال `FiscalPeriodsModal`. جناح `fiscal-periods.spec.ts` بنجاح 100%. |
| ~~**26**~~ | ~~مراجعة المرحلة 35 (الفترات المحاسبية الشهرية) وتحصينها~~ | ✅ **خلص (23 سبتمبر 2026 — Claude).** المرحلة نُفِّذت في جلسة أخرى، وهذه مراجعتها: ثلاث ملاحظات حقيقية أُصلحت — مفتاح أجنبي أحادي العمود أعاد فتح نمط O45 (هجرة 144 + تجربة فعلية للرفض من القاعدة)، وإعادة فتح الشهر كانت تمحو قفل الدفاتر اليدوي (FP-8)، وفحوص الإقفال كانت خارج المعاملة (FP-7). **الدرس:** التقرير المكتوب عن الشغل ليس الشغل — وُصفت في التقرير مفاتيح مركّبة و`ON DELETE RESTRICT` لم تكن في الكود. راجع الكود لا الوصف. **وأُغلقت معها الملاحظات الثلاث الأصغر (FP-9):** محرك توليد نقي يستورده الجناح، ومسار قراءة لا يكتب، وجناح واجهة لمودال الفترات |

---

## 3. بروتوكول التحديث — افعله في نهاية كل جلسة Opus

بعد ما بند يخلص:
1. حدّث الجدول في القسم 1 هنا (انقله من 🔴/🟡 لـ✅، أو حدّث "اللي اتراجع فعلاً")
2. سجّل التفاصيل التقنية (الثغرات، الإصلاحات، الثوابت الجديدة) في `ARCHITECTURE_INVARIANTS.md`
3. لو اتصلحت ثغرات، حدّث `SYSTEM_CAPABILITIES.md` لأي قدرة تغير سلوكها
4. اعمل commit بالتغييرات (لو مفيش commit لسه، الملفات هتفضل "معلقة" وأي diff مستقبلي هيبقى صعب التتبع)
5. لو البند فيه كود جديد اتلمس بعد المراجعة (زي حالة الـHR دلوقتي)، سجّل تاريخ/commit المراجعة عشان أي دلتا مستقبلية تتقاس منه
6. **شغّل `npm run guards` وتأكد إنه أخضر قبل ما تقول خلصت** — ولو قفلت ثغرة، سيب جناح يحرسها (القسم 0-ب)

---

## 4. الحالة عند `8dbb5586` (نقطة البداية لأي مراجعة جديدة)

بند 3 اتقسم على 5 كوميتات منطقية:

| Commit | المحتوى |
|---|---|
| `d9959572` | إدمبوتنسية منح الاشتراك + فاعل سجل التدقيق + هجرة 129 + المحرك والجناح |
| `5ae9fe86` | إزالة كلمة المرور الرئيسية المنشورة + توليد سر لكل تثبيت في الإلكترون |
| `557495bf` | إبطال الجلسات عند إعادة تعيين كلمة المرور + 3 عيوب دورة حياة المستأجر |
| `acf296d4` | ترميز صفحتي المحاكاة + تقييد وجهة إعادة التوجيه + الجناح |
| `8dbb5586` | اجتياز المسار في رفع التشخيص + توحيد سياسة كلمة المرور |

---

## 4-ب. الحالة عند `81961a7b` (بند 2)

بند 1 اتعمله commit واحد (`0a955e3b`) لأنه كان سايب الشجرة متغيرة بلا commit.
بند 2 اتقسم على 5 كوميتات منطقية منفصلة:

| Commit | المحتوى |
|---|---|
| `092b29a8` | عزل نقاط Webhook المتجر + المحرك النقي + الجناح الجديد |
| `f1ad4107` | `recordFieldCollection` وباقي كتابات `van-sales` |
| `0b0ef36c` | ثلاث جداول HR كانت بتتكتب بمستأجر `''` + هجرة 127 |
| `31f45c04` | بوابة إخلاء طرف العهد + طلبات الإجازة + `setEmployeeAssetStatus` |
| `bec17bef` + `81961a7b` | تهذيب 34 موضعاً كانوا آمنين بفحص سابق |

`tsc` نضيف على الباك إند والفرونت إند، و**33 جناح** `test:critical` بيعدوا 100%.
**من هنا فصاعداً، أي مراجعة جديدة تقيس نفسها بـ`git diff 81961a7b..HEAD`.**

> **تنبيه للجلسة الجاية:** أثناء بند 2 كانت فيه **جلسة تانية شغالة بالتوازي** على
> `contracting.service.ts` وملفات المقاولات في الفرونت إند (وأضافت هجرة 128).
> عشان كده بند 2 ماقربش من الملف ده خالص. اتأكد من `git status` قبل ما تبدأ.

---

## 5. البنود الجديدة وحالتها (تم تدقيق وإغلاق O21, O23, O25, O26, O37, O43, O49 بواسطة Antigravity)

| # | البند | راح لمين |
|---|---|---|
| ~~O21~~ | ~~تحصين الكنترولرز بـ`PermissionsGuard` لمنع غير المصرح لهم من إدارة الأقساط~~ | ✅ **اتقفل في البند 10 (بواسطة Antigravity)** |
| ~~O23~~ | ~~ثغرة تجاوز الباسكود في تحديثات الأوفلاين وحصرها ببيئة الديسكتوب المنعزلة~~ | ✅ **اتقفل في البند 10 (بواسطة Antigravity)** |
| ~~O25~~ | ~~نقاط Webhook المتجر الأربعة بلا تحقق توقيع (أو بفشل آمن معكوس)~~ | ✅ **اتقفل في البند 9 (بواسطة Antigravity)** |
| ~~O26~~ | ~~`database.types.ts` بيعلن أعمدة `tenant_id` مش موجودة في القاعدة~~ | ✅ **اتقفل في البند 10 (بواسطة Antigravity)** |
| O27 | `van-sales`: قراءة `customers` بلا نطاق + مفيش تحقق إن الرحلة تخص المندوب | مراجعة موديول المناديب |
| O28 | `rebuildPayrollRunItems` بيشتق النطاق من صف غير مُتحقَّق منه | البند 6 |
| ~~O43~~ | ~~التحقق الصارم من صحة النطاق الزمني `isValidTimezone` في `saveSettings`~~ | ✅ **اتقفل في البند 10 (بواسطة Antigravity)** |
| ~~O49~~ | ~~قصر احتساب إقرار القيمة المضافة على الفواتير المرحلة `status = 'posted'`~~ | ✅ **اتقفل في البند 11 (بواسطة Antigravity)** |

### اللي بند 3 فتحه

| # | البند | راح لمين |
|---|---|---|
| ~~O32~~ | ✅ **اتقفل** — قرار المالك: أصلحها مع احترام التفعيل اليدوي. **وتصحيح:** ماكانش تسريب إيرادي أصلاً؛ الـAPI كان محجوب صح بـ`PermissionsGuard`+`@RequireFeature`. الفعلي كان بقاء عناصر القائمة بعد التخفيض | — |
| O33 | `getMySubscription` مسار `GET` بيزوّد مستأجرين وخطط واشتراك 10 سنين | بند 9 (المتجر والتسعير) أو جلسة مستقلة |
| ~~O34~~ | ✅ **اتقفل** — قرار المالك: عمود جديد + تمريره للتدقيق. هجرة 131 + جناح `impersonation-audit-trail.spec.ts` | — |
| O35 | `MIN_PASSWORD_LENGTH = 1` على المشروع كله | 🟡 **قرار المالك: يفضل زي ما هو، ما يتزوّدش** |
| O36 | رفع ملفات التشخيص العام بلا حد معدّل | تنسيق مع عملاء الديسكتوب |
| ~~O37~~ | ~~ترقية ذاتية مجانية داخل المستأجر عبر مسار الـSandbox (مفيش `PermissionsGuard`)~~ | ✅ **اتقفل في البند 10 (بواسطة Antigravity)** |
| O38 | ترقية فهرس `(tenant_id, reference)` لـ`UNIQUE` بعد مطابقة التكرارات | بعد مطابقة بيانات فعلية |
| O39 | `AuthCacheService` بقيمة افتراضية في المُنشئ (فخ كامن) | تنظيف سريع |
| O40 | `developerUpdateTenantPlan` بيسقّط سجل التدقيق بصمت | تنظيف سريع |
