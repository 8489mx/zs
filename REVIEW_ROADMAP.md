# خريطة المراجعة الشاملة والنهائية (Review Roadmap)

> **الغرض:** هذا الملف هو نقطة الدخول الوحيدة لأي جلسة مراجعة جديدة (Opus أو غيره).
> افتح شات جديد، الصق رابط/محتوى هذا الملف، وقول: **"ابدأ بالبند رقم N"**.
> لما يخلص، حدّث حالته هنا، وافتح شات جديد للبند التالي.
>
> **هذا الملف لا يشرح الثغرات التفصيلية** (ده موجود في `ARCHITECTURE_INVARIANTS.md`).
> هو بس بيجاوب على سؤالين: **إيه اللي اتراجع فعلاً وموثوق فيه؟** و**إيه الأولوية التالية؟**

**آخر تحديث:** سبتمبر 2026 · **آخر Commit مرجعي:** `8dbb5586` (20 سبتمبر 2026)
**البند التالي: رقم 5** (باقي `maritime-freight.service.ts` — ~4100 سطر).
البند 1 (عزل المستأجرين) ✅ · البند 2 (O2 — الكتابة بلا `tenant_id`) ✅ **عدا 4 مواضع في المقاولات** · البند 3 (منصة SaaS) ✅
— شوف الجدول في القسم 1 و`ARCHITECTURE_INVARIANTS.md` §5-ب و§7 (المراحل 12 و13 و14).
أي حاجة جديدة تتضاف بعد الكوميت المرجعي، قِسها بـ:
`git diff 8dbb5586..HEAD -- <المسار>` — راجع القسم 0 قبل ما تبدأ أي بند.

---

## 0. القاعدة الذهبية — إزاي تتعامل مع موديول "مقفول" لما يتضاف له كود جديد

> **السؤال المتكرر:** موديول اتقفل ومراجَع بالكامل، وبعدين اتضاف له كود جديد. هل المراجعة الجديدة لازم تاخد الموديول كله تاني ولا بس الجزء الجديد؟

**الإجابة: لا الاتنين. القاعدة الوسطى هي الصح:**

❌ **مش "راجع الموديول كله من الأول"** — ده حرق توكن على كود اتأكد منه فعلاً وملوش داعي.
❌ **مش "راجع بس السطور اللي اتغيرت"** — لأن سطرين جداد جوه دالة قديمة من 40 سطر، الحكم عليهم صح ولا غلط **محتاج تشوف الدالة كلها**، مش السطرين لوحدهم. تغيير صغير جوه دالة كانت صح ممكن يكسر شرط أو يتخطى قفل كان موجود.

✅ **الصح:** **الـdiff + جسم أي دالة اتلمست بالكامل + هل الكود الجديد بيستخدم "مصادر الحقيقة" الموثّقة صح؟**

### خطوات عملية لأي مراجعة "دلتا" (Delta Review):

```bash
# 1. شوف بالظبط إيه اللي اتغير في الموديول ده منذ آخر مراجعة موثّقة
git diff <commit_آخر_مراجعة>..HEAD -- <مسار الموديول>

# لو لسه مفيش Commit للحالة الحالية (زي دلوقتي):
git diff --stat   # يوريك كل حاجة لسه معملهاش commit
```

2. **لأي دالة ظهرت في الـdiff:** اقرا الدالة **كاملة** (مش بس السطور الملونة)، واسأل:
   - هل التغيير ده بيحترم القفل/المعاملة (`transaction`/`forUpdate`) اللي كانت موجودة قبل كده؟
   - هل بيستخدم `applyStockDelta` / `insertPostedJournal` / المحرك النقي الصح، ولا بيكتب مباشر؟
   - هل بيضيف مسار جديد ممكن يتخطى بوابة رقابية كانت موجودة (زي فحص `tenant_id` أو حالة الفاتورة)؟

3. **لو الدالة نفسها منعرفتش تتغير خالص (0 تغيير فيها) وهي مش بتنادي على حاجة جديدة** — تقدر تثق فيها زي ما هي، من غير ما تعيد قراءتها.

### مثال حقيقي موجود دلوقتي في المشروع:
موديول الـHR **مراجَع ومقفول** (مرحلة 6 — لكن جزئياً، شوف القسم 1). دلوقتي فيه تعديلات جديدة غير محفوظة (`hr.service.ts`, `mobile-attendance.service.ts`, ملف جديد `tenant-timezone.util.ts`) بتضيف **مفهوم توقيت زمني لكل مستأجر بدل توقيت ثابت واحد للسستم كله** (كان `process.env.BUSINESS_TIMEZONE || 'Africa/Cairo'` ثابت، بقى `getTenantTimezone(tenantId)` ديناميكي). ده تغيير حقيقي في `listAttendance` و`upsertAttendanceRecord` — **لازم مراجعة دلتا مركّزة على الدالتين دول + ملف `tenant-timezone.util.ts` الجديد نفسه**، مش إعادة قراءة الـHR كله تاني، ومش تجاهله لأن "الموديول مقفول".

---

## 0-ب. بوابة الحُرّاس — شغّلها قبل وبعد أي شغل (سبتمبر 2026)

> **السبب:** كل أجنحة الاختبار الـ69 (`test/infra` + `test/critical`) وكل فحوص معمار الواجهة
> وكل أجنحة الواجهة (Vitest) كانت موجودة و**خارج الـCI**. يعني المراجعة اليدوية بتقفل باب،
> وأول ميزة جديدة تقدر تفتحه تاني بصمت.
> أول ما اتوصلت البوابة، طلعت **3 أجنحة باك إند حمراء** (انكسرت بميزات سابقة ومحدش لاحظ)
> و**18 اختبار واجهة فاشل** منهم انحدار صلاحيات حقيقي (البند O29) وكسر فعلي في بناء الواجهة.

**الحالة دلوقتي — البوابة كلها خضرا وموصّلة:**

| الجزء | العدد | في الـCI |
|---|---|---|
| أجنحة الباك إند (`test:infra` وجواها `test:critical`) | 69 | ✅ |
| أجنحة الواجهة (Vitest) | **246 اختبار / 55 ملف** | ✅ |
| فحوص معمار الواجهة والعقود | 6 | ✅ |
| `frontend npm run lint` | 22,451 ملاحظة | ❌ **الوحيد اللي لسه بره** |

```bash
npm run guards            # نفس اللي الـCI بيشغّله بالضبط — من جذر المستودع، بلا build
npm run guards:backend    # الـ69 جناح لوحدهم (أسرع، ومعظم الثغرات المالية هنا)
npm run guards:frontend   # فحوص المعمار والعقود + الـ246 اختبار واجهة
```

**التفاصيل الكاملة (ما اتوصّل، السقّاطة، الأخطاء اللي التصفير كشفها، اللي لسه أحمر):**
`ARCHITECTURE_INVARIANTS.md` §6-ج. **البنود اللي فتحها:** O30 (إتاحة `Field`) · O31 (نصوص
بتعرض `${...}` حرفياً للمستخدم).

### تعريف "خلصت" لأي ميزة جديدة أو تعديل

1. `npm run guards` **أخضر** قبل ما تبدأ (عشان تعرف إن أي أحمر بعدين بسببك إنت).
2. اتبع قاعدة الدلتا في القسم 0 تحت لأي موديول مقفول اتلمس.
3. الكتابة بتمر من مصادر الحقيقة (`ARCHITECTURE_INVARIANTS.md` §2)؟
4. **سيب حارس مكان الباب اللي قفلته:** أي ثغرة تتصلح أو قاعدة جديدة تتفرض، يمشي معاها تأكيد في
   جناح — وإلا هترجع تاني بعد شهرين وإنت مش واخد بالك. (مثال حي: `UpsertUserDto` بقى بيفرض
   `phone` ومحدش حرسها، فالجناح فضل أحمر بصمت لحد النهاردة.)
5. `npm run guards` **أخضر** قبل ما تقول خلصت.

**السقّاطة (Ratchet):** فحوص الواجهة الخمسة اللي عليها دين تاريخي بتفشل على الجديد بس.
توسيع أي ملف في `frontend/scripts/baselines/` مش إصلاح — ده قرار محتاج مبرر.

**تحذير مهم لما جناح يبقى أحمر:** أول سؤال هو **«الكود غلط ولا الجناح قديم؟»** — مش تعديل
الجناح على طول. في جولة التصفير دي، من الـ18 الفاشلين كان **6 منهم انحدار حقيقي في الكود**
(الأدمن بيتخطى صلاحياته) — لو اتعدّلوا كأنهم أجنحة قديمة كنا هنثبّت الثغرة بدل ما نصلّحها.
وواحد كان **ميزة اتشالت عمداً** (بانر «لا يوجد اتصال» — قرار منتج مؤكَّد، متُرجّعش).

---

## 1. الحالة الحالية — إيه اللي موثوق فيه فعلاً (بالدقة الكاملة)

### ✅ اتراجع سطر بسطر وخلص (مُقفَل)
| الموديول | نطاق المراجعة الفعلي | آخر حالة معروفة |
|---|---|---|
| المقاولات — المستخلص والضمانات | `ipc-calculation.engine.ts`, `guarantee-gateway.engine.ts`, مسارات الاستدعاء | ✅ 5 جولات، I1-I12 وG1 مُختبَرة |
| نقطة البيع والورديات | `cash-drawer.service.ts`, `sales-write.service.ts` بالكامل | ✅ 4 ثغرات اتصلحت |
| المحاسبة العامة | `accounting.service.ts`, `fiscal-year.service.ts`, `forex-revaluation.service.ts` | ✅ 7 ثغرات اتصلحت |
| المشتريات | `purchase-orders.service.ts`, `goods-receipt.service.ts`, `three-way-match.engine.ts`, `purchase-landed-costs.service.ts` | ✅ 23 ثغرة اتصلحت |
| المخزون والحركات | `van-sales.service.ts`, `inventory-transfer.service.ts`, مسار استلام أمر الشراء، `catalog-product.service.ts` (الرصيد الافتتاحي) | ✅ 4 ثغرات + جرد تسوية (هجرة 115) |
| **آلية عزل المستأجرين والحُرّاس** (البند 1) | `tenant-boundary.ts`, `tenant-context.ts`, `csrf-token.ts`, الحُرّاس الخمسة, `session.service.ts:resolveAuthContext`, `auth-cache.service.ts` + **مسح 78 Controller / 941 مساراً** وقراءة يدوية للـ73 مساراً بلا حارس | ✅ **الآلية نفسها سليمة · 8 ثغرات اتصلحت كلها في البوابات اللي بتلتف عليها** · جناح `portal-token.spec.ts` جديد · **مراجَع اعتباراً من `a7236d05` (20 سبتمبر 2026)** |
| **الكتابة بلا `tenant_id` — البند O2** (البند 2) | مسح **707** موضع `updateTable`/`deleteFrom` + كل `sql\`UPDATE/DELETE\`` الخام + كل `INSERT` في `backend/src`. قراءة يدوية لكل موضع من الـ**90** اللي طلعوا بلا نطاق، + قراءة كل كاتب لكل جدول اتلمس قبل أي تضييق | ✅ **3 ثغرات عزل حقيقية + 5 ميزات ميتة بصمت اتصلحت** · هجرة 127 للترميم · جناح `webhook-order-resolution.spec.ts` جديد · **متبقي 4 مواضع في `contracting.service.ts`** (جلسة تانية كانت بتعدّل الملف) · **مراجَع اعتباراً من `81961a7b` (20 سبتمبر 2026)** |
| **منصة SaaS** (البند 3) | `saas-admin` (2522 سطر) + `tenant-subscription` (1676) + `activation` (469) — قراءة كاملة سطراً بسطر، شاملة الحُرّاس والـDTOs وبوابات الدفع الثلاث، + فحص مُوجَّه لمسار التسجيل التجريبي العام | ✅ **9 ثغرات اتصلحت، 3 منهم حرجة:** تراكم فترات الاشتراك من إعادة إرسال الويب هوك · كلمة مرور رئيسية منشورة في الكود بتفتح كل الموديولات المدفوعة · اجتياز مسار غير مُصادَق بيكتب ملف بره شجرة التخزين. هجرة 129 · جناحين جداد · **مراجَع اعتباراً من `8dbb5586` (20 سبتمبر 2026)** |
| **دلتا التوقيت الزمني في الحضور** (البند 4) | `tenant-timezone.util.ts` بالكامل + أجسام `upsertAttendanceRecord` و`listAttendance` و`recomputeAttendanceDerivedFields` كاملة + مسار الكتابة في `saveSettings` + حمولات الواجهة | ✅ **الميزة سليمة وموصولة · 6 أعطال في الحواف اتصلحت** · محرك نقي `attendance-punch.engine.ts` + جناح مربوط بعد ما كان يتيم ويؤكد عكس الإنتاج |

### 🟡 اتلمست جزئياً — ثقة **جزء صغير بس**، الباقي غير مقروء
| الموديول | اللي اتراجع فعلاً | اللي لسه مقفول (محدش فتحه) |
|---|---|---|
| الشحن البحري (`maritime-freight.service.ts`) | فاتورة الناقل، فاتورة العميل، سند المصروفات (~500 سطر) — **4 ثغرات مالية حقيقية اتصلحت** | باقي **~4100 سطر**: RFQ، الحاويات، البيانات الجمركية، التأمين، إيصالات المستودع |
| الرواتب (`hr.service.ts`) | منطق أولوية خصم القرض (~100 سطر) — ثغرة اتصلحت. **+ دلتا التوقيت الزمني والحضور خلصت في بند 4** (`upsertAttendanceRecord`, `listAttendance`, `recomputeAttendanceDerivedFields`, `tenant-timezone.util.ts` — 6 أعطال اتصلحت). **+ `employee-portal.service.ts` اتراجعت منه طبقة المصادقة والعزل بالكامل في بند 1** (الرموز + كل الاستعلامات + الإدراجات) | باقي **~3700 سطر** من `hr.service.ts`: مكافأة نهاية الخدمة، العهد، مسيّر الرواتب نفسه. ومن `employee-portal.service.ts`: **منطق `login` التفصيلي ومطابقة الـPIN عبر المستأجرين** (البند O20 — PIN نصي صريح) |
| الخزينة والذمم (`payment-allocation.service.ts`) | سباق تخصيص المدفوعات — ثغرة اتصلحت | `treasury.service.ts` و`tax-integration` بالكامل — صفر |
| التسعير والمتجر (`storefront.service.ts`) | كود الكوبونات — سباق اتصلح | `pricing.service.ts`, `crm.service.ts`, `marketplace-sync.service.ts` — صفر |
| الوحدات القطاعية | التصنيع (فُحص، طلع "ميزة ناقصة" O8)، FEFO الصيدليات (فُحص، سليم) | `maintenance.service.ts` بالكامل — صفر |

### 🔴 صفر قراءة — فيه تست بيعدي بس محدش قراه
| الموديول | ملاحظة |
|---|---|
| `settings` (7709 سطر) | آلية عزل المستأجرين الأساسية (`tenant-boundary.ts`) **مقفولة ومراجَعة بالكامل في بند 1**، لكن ملف `settings` نفسه (RBAC، إعدادات الفروع) صفر قراءة. **بند 1 سلّم له 3 بنود محددة:** O21 (18 Controller بـ`SessionAuthGuard` بلا `PermissionsGuard`)، O22 (نقاط `health` التشغيلية بلا حارس)، O23 (فشل آمن معكوس في `passcode` تحديثات الأوفلاين) |
| `reports.service.ts` (3469 سطر) | فحص مُوجَّه بالـgrep بس (فلاتر `status`/`tenant_id`) — مش قراءة كاملة |
| `contracting.service.ts` باقي الملف | 13 ألف سطر، اتراجع منها بس المستخلص والضمانات. **دين تقني موثّق:** 172 استخدام لـ`(this.db as any)` — البند O16 |

---

## 2. قائمة الأولويات — رقم واحد لحد ما نخلص

> نفّذهم بالترتيب ده. كل بند = شات Opus جديد منفصل. بعد ما يخلص، حدّث ✅/🟡/🔴 في الجدول فوق و في `ARCHITECTURE_INVARIANTS.md`.

| # | البند | ليه الأولوية دي | نوع المراجعة |
|---|---|---|---|
| ~~**1**~~ | ~~آلية عزل المستأجرين نفسها (`core/auth/utils/tenant-boundary.ts` + الـGuards)~~ | ✅ **خلص (20 سبتمبر 2026، مرجعه `a7236d05`).** الآلية نفسها طلعت سليمة؛ الثغرات الـ8 كانت كلها في **البوابات اللي بتلتف عليها عمداً** (`employee-portal`, `mobile-attendance`, `driver-portal`): سر توقيع افتراضي منشور في الكود (تزوير هوية عابر للمستأجرين)، رموز 30 يوم بلا إبطال، استعلامات بوابة الموظف كلها بلا `tenant_id`، `INSERT` طلبات الإجازة والسلفة بمستأجر `''`، وجدول `hr_leave_balances` غير موجود أصلاً. التفاصيل في `ARCHITECTURE_INVARIANTS.md` §5-ب | ✅ منجز |
| ~~**2**~~ | ~~البند O2: مواضع `UPDATE`/`DELETE` بلا `tenant_id` صريح~~ | ✅ **خلص (20 سبتمبر 2026، مرجعه `81961a7b`).** الرقم الحقيقي **90** موضعاً (مش ~70 ولا ~212). **الدرس الأهم من البند:** أغلب الـ90 كانت آمنة فعلاً بفحص ملكية سابق — الخطر الحقيقي كان في اتجاه تاني خالص: `INSERT` بيغفل `tenant_id` على عمود `NOT NULL DEFAULT ''` (F18)، فالصف بيتكتب بمستأجر `''`، وبعدين قارئ بيُرشِّح بالعمود ما بيلاقيهوش — **5 ميزات كانت ميتة بصمت**، منهم **بوابة إخلاء طرف العهد** (مخالصة نهاية خدمة كانت تتصرف كاملة لموظف لسه بعهدته أصول) و**اعتماد الأوفرتايم** (ما كانش بيوصل مسيّر الرواتب أبداً). الثغرات الحقيقية الـ3: حل طلب الدفع عابراً للمستأجرين في 4 نقاط Webhook عامة · `recordFieldCollection` بيكتب رحلة أي مستأجر · `setEmployeeAssetStatus` بيقرا ويكتب عهدة أي مستأجر. التفاصيل في `ARCHITECTURE_INVARIANTS.md` §5-ب و§7 (المرحلة 13) | ✅ منجز |
| ~~**3**~~ | ~~منصة SaaS (`saas-admin` + `tenant-subscription` + `activation`)~~ | ✅ **خلص (20 سبتمبر 2026، مرجعه `8dbb5586`).** **الدرس الأهم:** أخطر حاجة في طبقة الإيراد ماكانتش منطق التسعير، كانت **غياب مفتاح إدمبوتنسي على حدث خارجي** — كل بوابة دفع بتعيد إرسال الويب هوك، وتوقيع XPay/Paymob هو HMAC على الجسم بلا nonce، فحمولة واحدة مُلتقطة تتعاد N مرة = N سنة اشتراك مجانية. والأخطر إن **خطأ في فاعل سجل التدقيق بعد المعاملة** كان بيخلي كل دفعة شرعية ترجع 500 فتعيد البوابة المحاولة فيتضاعف المنح — يعني العطل كان بيشغّل نفسه على كل عميل بيدفع، مش كامن. وتانيها: `DEVELOPER_MASTER_PASSWORD || 'infoadmin'` منشورة في الكود ومشحونة مع كل نسخة ديسكتوب بتستمع على `0.0.0.0` في وضع الشبكة. وتالتها: اجتياز مسار غير مُصادَق في رفع ملفات التشخيص. التفاصيل في `ARCHITECTURE_INVARIANTS.md` §5-ب و§7 (المرحلة 14) | ✅ منجز |
| ~~**4**~~ | ~~**دلتا الرواتب الجديدة** (توقيت زمني لكل مستأجر — `tenant-timezone.util.ts`, التعديلات في `hr.service.ts`/`mobile-attendance.service.ts`)~~ | ✅ **خلص (20 سبتمبر 2026).** الدلتا = كوميتان: `49290f29` (الميزة) و`cccfdae7` (مرساة وقت السيرفر). الميزة نفسها سليمة وموصولة؛ **6 أعطال في الحواف**، أخطرها: **مرساة وقت السيرفر كانت نصف موصولة** — تثبّت لحظة البصمة وتترك `work_date` (اليوم الذي يقرؤه مسيّر الرواتب) بيد العميل. و**الجناح المرافق كان يعيد كتابة المنطق داخل ملف الاختبار ويؤكد سلوكاً معاكساً لما ينفّذه الإنتاج، وكان غير مربوط بأي سكربت npm فلم يُشغَّل قط.** كمان: قراءة-ثم-كتابة بلا معاملة في مسار البصمة (F1)، وفرع الوردية الجديدة يبتلع طلب الانصراف. التفاصيل في `ARCHITECTURE_INVARIANTS.md` §5-ب و§7 (المرحلة 15) | ✅ منجز |
| **5** | باقي `maritime-freight.service.ts` (~4100 سطر) | عندنا دليل فعلي (4 ثغرات) إن الملف ده فيه نمط أخطاء متكرر — أعلى احتمال تلاقي حاجة تانية | قراءة كاملة |
| **6** | باقي `hr.service.ts` (~3900 سطر) + باقي `employee-portal.service.ts` | فلوس فعلية شهرية، وعندنا سابقة باج (خصم القرض). **طبقة المصادقة والعزل في البوابة خلصت في بند 1** — الباقي هنا هو منطق الرواتب نفسه + **البند O20 (تجزئة الـPIN بدل التخزين النصي) اللي بند 1 أجّله له عمداً** لأنه محتاج هجرة + إعادة تصميم مسار الدخول | قراءة كاملة  **+ البند O28 من بند 2:** `rebuildPayrollRunItems` بيقرا `hr_payroll_runs` بلا `tenant_id` وبيشتق منه النطاق (محاصَر دلوقتي بفحص المستدعين). **ملحوظة مهمة:** بند 2 لمس `hr.service.ts` في مواضع محددة بس (الـ`INSERT`ات اللي كانت بتغفل النطاق + مواضع O2) — باقي الملف لسه صفر قراءة |
| **7** | `treasury.service.ts` + `tax-integration` | نقدية مباشرة + امتثال ضريبي/قانوني (ZATCA/ETA) | قراءة كاملة |
| **8** | باقي `contracting.service.ts` + إزالة الـ172 `as any` (O16) | أكبر سطح مالي في المشروع (13 ألف سطر) ودين تقني موثّق بيخفي بالظبط نوع الأخطاء اللي لقيناها في `forex-revaluation` — لكن نطاق تأثيرها محصور في نشاط المقاولات وحده | قراءة كاملة (ممكن تتقسم لعدة جلسات)  **+ آخر 4 مواضع من البند O2** (`contracting_master_boq_library` ×3 و`contracting_supplier_price_memory` ×1): اتفحصوا في بند 2 وطلعوا آمنين بفحص ملكية سابق، وما اتلمسوش **بس** لأن جلسة تانية كانت بتعدّل نفس الملف وقت المراجعة. إضافة `.where('tenant_id', ...)` عليهم شغل 10 دقايق |
| **9** | باقي `pricing.service.ts` / `crm.service.ts` / `storefront.service.ts` | تأثير مالي أقل مباشرة من ترحيل قيود. **+ البند O25 اللي بند 2 سلّمه:** نقاط Webhook المتجر الأربعة (`storefront-payment.service.ts`) **ما بتتحققش من توقيع إطلاقاً** (XPay/Tap/Stripe) أو بتتحقق بفشل آمن معكوس (Paymob). انتبه: جناح `phase12` بيغطي بوابات **اشتراكات المنصة** مش الملف ده — سطحان مختلفان بنفس الاسم | قراءة كاملة |
| **10** | `settings` (7709 سطر) — RBAC وإعدادات الفروع (بعيداً عن آلية العزل الأساسية اللي في بند 1) | حجم كبير، خطورة متوسطة. **+ O21/O22/O23 من بند 1، و O26 من بند 2** (`database.types.ts` بيعلن `tenant_id`/`account_id` على `OfflineReleaseTable` و`ManufacturingBomLineTable` والعمودين مش موجودين في القاعدة أصلاً — فخ لأي حد يحاول "يقفل موضع O2" عليهم) | قراءة كاملة (تتقسم لجلسات)  **+ البند O37 من بند 3:** `sandbox-checkout/complete` بيسمح لأي مستخدم مُصادَق بأي دور إنه يمنح نفسه أي باقة لما مفيش مفاتيح بوابة مضبوطة (مفيش `PermissionsGuard` على أي مسار في `tenant-subscription.controller.ts`) |
| **11** | `reports.service.ts` (3469 سطر) | قراءة فقط، أقل خطورة مالية مباشرة | قراءة كاملة |
| **12** | `maintenance.service.ts` | صفر قراءة، حجم أصغر نسبياً | قراءة كاملة |

---

## 3. بروتوكول التحديث — افعله في نهاية كل جلسة Opus

بعد ما بند يخلص:
1. حدّث الجدول في القسم 1 هنا (انقله من 🔴/🟡 لـ✅، أو حدّث "اللي اتراجع فعلاً")
2. سجّل التفاصيل التقنية (الثغرات، الإصلاحات، الثوابت الجديدة) في `ARCHITECTURE_INVARIANTS.md`
3. لو اتصلحت ثغرات، حدّث `SYSTEM_CAPABILITIES.md` لأي قدرة تغير سلوكها
4. اعمل commit بالتغييرات (لو مفيش commit لسه، الملفات هتفضل "معلقة" وأي diff مستقبلي هيبقى صعب التتبع)
5. لو البند فيه كود جديد اتلمس بعد المراجعة (زي حالة الـHR دلوقتي)، سجّل تاريخ/commit المراجعة عشان أي دلتا مستقبلية تتقاس منه
6. **شغّل `npm run guards` وتأكد إنه أخضر قبل ما تقول خلصت** — ولو قفلت ثغرة، سيب جناح يحرسها (القسم 0-ب)

---

## 4. الحالة عند `8dbb5586` (نقطة البداية لأي مراجعة جديدة)

بند 3 اتقسم على 5 كوميتات منطقية:

| Commit | المحتوى |
|---|---|
| `d9959572` | إدمبوتنسية منح الاشتراك + فاعل سجل التدقيق + هجرة 129 + المحرك والجناح |
| `5ae9fe86` | إزالة كلمة المرور الرئيسية المنشورة + توليد سر لكل تثبيت في الإلكترون |
| `557495bf` | إبطال الجلسات عند إعادة تعيين كلمة المرور + 3 عيوب دورة حياة المستأجر |
| `acf296d4` | ترميز صفحتي المحاكاة + تقييد وجهة إعادة التوجيه + الجناح |
| `8dbb5586` | اجتياز المسار في رفع التشخيص + توحيد سياسة كلمة المرور |

---

## 4-ب. الحالة عند `81961a7b` (بند 2)

بند 1 اتعمله commit واحد (`0a955e3b`) لأنه كان سايب الشجرة متغيرة بلا commit.
بند 2 اتقسم على 5 كوميتات منطقية منفصلة:

| Commit | المحتوى |
|---|---|
| `092b29a8` | عزل نقاط Webhook المتجر + المحرك النقي + الجناح الجديد |
| `f1ad4107` | `recordFieldCollection` وباقي كتابات `van-sales` |
| `0b0ef36c` | ثلاث جداول HR كانت بتتكتب بمستأجر `''` + هجرة 127 |
| `31f45c04` | بوابة إخلاء طرف العهد + طلبات الإجازة + `setEmployeeAssetStatus` |
| `bec17bef` + `81961a7b` | تهذيب 34 موضعاً كانوا آمنين بفحص سابق |

`tsc` نضيف على الباك إند والفرونت إند، و**33 جناح** `test:critical` بيعدوا 100%.
**من هنا فصاعداً، أي مراجعة جديدة تقيس نفسها بـ`git diff 81961a7b..HEAD`.**

> **تنبيه للجلسة الجاية:** أثناء بند 2 كانت فيه **جلسة تانية شغالة بالتوازي** على
> `contracting.service.ts` وملفات المقاولات في الفرونت إند (وأضافت هجرة 128).
> عشان كده بند 2 ماقربش من الملف ده خالص. اتأكد من `git status` قبل ما تبدأ.

---

## 5. البنود الجديدة اللي بند 2 فتحها

| # | البند | راح لمين |
|---|---|---|
| O25 | نقاط Webhook المتجر الأربعة بلا تحقق توقيع (أو بفشل آمن معكوس) | البند 9 |
| O26 | `database.types.ts` بيعلن أعمدة `tenant_id` مش موجودة في القاعدة | البند 10 |
| O27 | `van-sales`: قراءة `customers` بلا نطاق + مفيش تحقق إن الرحلة تخص المندوب | مراجعة موديول المناديب |
| O28 | `rebuildPayrollRunItems` بيشتق النطاق من صف غير مُتحقَّق منه | البند 6 |

### اللي بند 3 فتحه

| # | البند | راح لمين |
|---|---|---|
| O32 | تخفيض الباقة ما بيطفّيش الموديولات (والمزامنة كلها في `try/catch` صامت) | قرار منتجي + جلسة مستقلة |
| O33 | `getMySubscription` مسار `GET` بيزوّد مستأجرين وخطط واشتراك 10 سنين | بند 9 (المتجر والتسعير) أو جلسة مستقلة |
| O34 | انتحال شخصية المالك بلا أثر تدقيق مميَّز (محتاج عمود في المخطط) | تصميم مستقل |
| O35 | `MIN_PASSWORD_LENGTH = 1` على المشروع كله | **قرار سياسة منتج** |
| O36 | رفع ملفات التشخيص العام بلا حد معدّل | تنسيق مع عملاء الديسكتوب |
| O37 | ترقية ذاتية مجانية داخل المستأجر عبر مسار الـSandbox (مفيش `PermissionsGuard`) | البند 10 (RBAC) |
| O38 | ترقية فهرس `(tenant_id, reference)` لـ`UNIQUE` بعد مطابقة التكرارات | بعد مطابقة بيانات فعلية |
| O39 | `AuthCacheService` بقيمة افتراضية في المُنشئ (فخ كامن) | تنظيف سريع |
| O40 | `developerUpdateTenantPlan` بيسقّط سجل التدقيق بصمت | تنظيف سريع |
