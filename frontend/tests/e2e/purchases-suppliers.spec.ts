import { test, expect } from '@playwright/test';

test.describe('Purchases and Suppliers Flow', () => {
  test('should create a purchase invoice and verify supplier balance', async ({ page }) => {
  });
});
