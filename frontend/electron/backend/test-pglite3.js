const { PGlite } = require('@electric-sql/pglite'); async function run() { const path = require('path'); const dbPath = path.join(process.cwd(), 'portable_data_test'); console.log('dbPath:', dbPath); const db = new PGlite('file://' + dbPath); await db.query('CREATE TABLE IF NOT EXISTS users2 (id INT)'); const res = await db.query('SELECT * FROM users2'); console.log(res); } run();
