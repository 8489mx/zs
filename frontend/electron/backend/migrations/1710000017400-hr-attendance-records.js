"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.migration = void 0;
const kysely_1 = require("kysely");
const upStatements = [
    `CREATE TABLE IF NOT EXISTS hr_attendance_records (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    employee_id BIGINT NOT NULL REFERENCES hr_employees(id) ON DELETE CASCADE,
    work_date DATE NOT NULL,
    status VARCHAR(32) NOT NULL DEFAULT 'present',
    check_in_at TIMESTAMPTZ NULL,
    check_out_at TIMESTAMPTZ NULL,
    source VARCHAR(32) NOT NULL DEFAULT 'manual',
    notes TEXT NULL,
    created_by BIGINT NULL REFERENCES users(id) ON DELETE SET NULL,
    updated_by BIGINT NULL REFERENCES users(id) ON DELETE SET NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT hr_attendance_records_employee_work_date_unique UNIQUE (employee_id, work_date),
    CONSTRAINT hr_attendance_records_status_valid CHECK (status IN ('present','absent','late','half_day','leave','excused','early_leave')),
    CONSTRAINT hr_attendance_records_source_valid CHECK (source IN ('manual','import'))
  )`,
    'CREATE INDEX IF NOT EXISTS idx_hr_attendance_records_work_date ON hr_attendance_records(work_date)',
    'CREATE INDEX IF NOT EXISTS idx_hr_attendance_records_employee_id ON hr_attendance_records(employee_id)',
    'CREATE INDEX IF NOT EXISTS idx_hr_attendance_records_status ON hr_attendance_records(status)',
];
const downStatements = [
    'DROP TABLE IF EXISTS hr_attendance_records',
];
exports.migration = {
    up: async (db) => {
        for (const statement of upStatements) {
            await kysely_1.sql.raw(statement).execute(db);
        }
    },
    down: async (db) => {
        for (const statement of downStatements) {
            await kysely_1.sql.raw(statement).execute(db);
        }
    },
};
//# sourceMappingURL=1710000017400-hr-attendance-records.js.map